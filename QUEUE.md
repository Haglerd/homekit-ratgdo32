# homekit-ratgdo32 work queue

Priority-ordered. Top = next. Detailed analysis lives in `audit-notes/` (gitignored fork-internal). Done items are removed — git log is the durable record.

## Active

### [P0] log-audit-20260518-004 — .86 `heap_caps_register_failed_alloc_callback` triggered 25-fire reboot storm over 27 min post-OTA (2026-05-18T06:23 -> 06:50 CDT, all sz=2308B)
**Status:** shipped revert in .87 (PR #138) — failed-alloc callback portion removed; HAP gate retained; **3.5h post-.87 soak shows ZERO failed-alloc fires, ZERO unexpected reboots** (log-audit 2026-05-18 11:11 CDT). Storm regression confirmed dead; 72h soak still pending for full acceptance.
**Source:** log-audit 2026-05-18 22:30 (Pi syslog 24h, .86 OTA at 06:21:57)
**Issue:** (not filed — auto-mode denied, queue-only)
**Acceptance:** revert the failed-alloc callback portion of PR #137 (keep the HAP heap-pressure gate). Build .87, OTA flash, soak 72h with zero `Heap-cap failed-alloc callback fired` lines and zero unexpected reboots, crashCount=0.
**Notes:** P0 user-visible failure. 25 fires from 06:23:21 to 06:50:25 CDT on 2026-05-18, all identical sz=2308B, each triggering `graceful restart in 2s`. Storm self-resolved at 06:50 and device has been stable since (uptime=21908s/~6.08h at audit time, crashCount=0, freeHeap=80216, minHeap=64436, min_free_heap_ever=15540 from /heap). Pattern: WiFi establish -> HomeSpan setup -> ~55-180s -> 2308B alloc fails -> restart -> loop. Each boot reports `Free heap at boot: 193KB` then `Current free heap: 82KB` (60% consumed by HomeSpan/HK 4-controller reattach before the fire). The callback is firing on a recoverable transient (same allocator class as the .85 sub-1KB events, likely HAP TempBuffer/pairing-verify/curve25519) but reacting with immediate restart, and the next boot encounters the same transient — net result MORE harmful than the original threat. Pre-.86 behavior under the same transient class was: 1Hz heap-watermark log + adaptive 30s cadence + zero crash. **Recommend option 1: revert the callback entirely, keep the HAP gate (read-side observability that can't make things worse).** Secondary: instrument the 2308B allocator (likely HAP TempBuffer/pairing-verify) before re-introducing any callback-driven action. Alternative paths considered: (2) tolerate N fires in sliding window, (3) skip callback for first 5-10 min post-boot. Option 1 is the cleanest revert path with the least new code. Auto-fix-eligibility: **needs-human-planning** — touches the in-flight `fix/hap-heap-pressure-gate` branch, revert decision needs user sign-off.

### [P2] log-audit-20260519-001 — HomeKit iOS-quiet watchdog escalations trending up on .87 — level-3 (`No Response` band) rate ~3.4x prior day, one 88-min quiet stretch
**Status:** deferred (cross-audit watch — re-evaluate after 2-3 more log-audits per acceptance; not drain-actionable until a trend is confirmed)
**Source:** log-audit 2026-05-19 04:28 (Pi syslog, window 2026-05-18T22:00Z → 2026-05-19T04:28Z, ~6.5h, 983 lines; device endpoints empty — passwordRequired=true blocks /showlog Digest auth)
**Issue:** (not filed — auto-mode, queue-only)
**Acceptance:** classify whether the rising `last_hap_read_ago` escalation rate is (a) normal iOS hub idle behavior under longer overnight quiet periods, or (b) a degrading mDNS/HAP connection that the watchdog hint is correctly leading. Resolves to "no action" if the device keeps self-recovering with recoverAttempts=0 and no actual `HomeKit pair lost` / `No Response` confirmation. If level-3 frequency keeps climbing across the next 2-3 audits, escalate to P1 and investigate mDNS announce cadence / HAP keep-alive on .87.
**Notes:** P2 — observability only; device is healthy (crashCount=0, freeHeap=73296, heap baseline steady ~42KB, recoverAttempts=0, hkAutoRecover=false so no recovery action taken regardless). All 11 quiet episodes self-recovered ("HAP reads resumed", levels 1/2/3). Rate trend vs prior 22h window (2026-05-18T00:00→21:59, on .87, same continuous boot): L1 2.4/h→1.7/h (down), L2 0.64/h→1.08/h (~1.7x), L3 0.18/h→0.62/h (~3.4x). Four level-3 events this window: 00:29:58 (ago=1829s), 01:15:45 (ago=1820s), 03:14:56 (ago=1879s), 03:56:00 (ago=1807s) CDT. Longest quiet stretch: L3 at 01:15:45 did not clear until "recovering from level 3" at 02:43:58 — 88 minutes. No WiFi flap (0 disconnects), RSSI steady -40dBm, no pair loss, no SSE desync — so this is iOS hub-side quiet, not a device WiFi/heap fault. The escalation is overnight-concentrated (all 4 L3 between 00:30 and 04:00 CDT) which is consistent with the iOS home hub entering deep idle; the watchdog hint text itself says "could be normal hub idle." Genuinely new finding — no existing queue entry tracks quiet-hint escalation rate. Auto-fix-eligibility: **observability-only** — no fix queued; tracked for cross-audit trend confirmation.

### [P1] log-audit-20260517-001 — sub-1KB heap transient (free=864 maxBlock=372 → updated to 528B intrinsic on .85) — underlying allocator burst
**Status:** shipped in .86 (PR #137) — HAP preflight gate + failed-alloc safety net; soak validation pending
**Source:** log-audit 2026-05-17 05:00 (5h post-.84 boot, 180 lines), recurring + deeper in log-audit-2026-05-18 (24h window: 4 sub-2KB events, deepest free=800 maxBlock=372; plus `min_free_heap_ever=528 B` from `/heap` IDF intrinsic — see log-audit-20260518-002)
**Issue:** (not filed)
**Acceptance:** identify what's consuming heap during these sub-second transients OR confirm the existing recovery path (panic-loop-free for 7+ events) is sufficient and downgrade to observability-only. 10+ sub-1KB transients now observed across .84/.85 with only ONE crash, and that crash was TASK_WDT (timing) not OOM (allocator) — strengthens "recovery path is sufficient" path. **2026-05-19: instrumentation paths are exhausted** — heap-trace is infeasible (precompiled arduino-framework libs ship `CONFIG_HEAP_TRACING_OFF=1`, no `heap_trace.h`, no linkable `heap_trace_*` symbols; unlocking it needs an espidf-framework migration, out of scope) and `heap_caps_register_failed_alloc_callback` is disqualified by log-audit-20260518-004 (the .86 reboot storm). Path (b) "confirm recovery sufficient" is therefore the only live path; the .86 HAP preflight gate is the read-side mitigation already shipped.
**Notes:** P1 — `.84/.85`'s 1Hz heap-watermark check WORKS (catches the dip, fast-cadence arms, recovers within 27-30s, no OOM panic). **2026-05-18 update — diagnosis correction**: the prior status header attributed the 2026-05-17T08:20 reboot to "HomeSpan HAP TempBuffer ctor at free=324B" — that was conjecture. The actual `esp_reset_reason()` value was 6 = ESP_RST_TASK_WDT (task watchdog timeout), not 4 = ESP_RST_PANIC (allocator abort) or 9 = ESP_RST_BROWNOUT. Pre-crash heap was 42268B (healthy) at 08:19:53, with reboot 18s later at 08:20:11. The TASK_WDT class of crash is now tracked as a peer P0 finding (log-audit-20260518-001) — heap pressure may be a contributing factor (lwIP/WiFi spin under transient OOM cascading into TWDT) but the root cause is task-blocked, not allocation-failed. **2026-05-18 deepest-event update**: free=800 maxBlock=372 at 02:24:49 CDT (.84, 64B below the prior 864B record), free=804 at 04:17:55 CDT, free=1580 at 09:59:25 CDT, free=1616 at 09:28:06 CDT — 4 sub-2KB events in 24h. Plus the IDF intrinsic `min_free_heap_ever=528 B` from /heap on .85 (sub-Hz dip below the 1Hz polling resolution — separate evidence tracked as log-audit-20260518-002). RSSI consistently -34 to -39dBm (good), no concurrent door activity, no SEC1 retry preceding. Same allocator likely also responsible for the 30s-cadence 15-25KB shallow dips observed on .85 (log-audit-20260518-003). Suspect: HomeKit pairing-verify, HAP-event-bulk-notify (4 controllers paired), TLS/curve25519 transient, or mDNS query burst. Recovery proves 1Hz check is the right safety net but doesn't identify the allocator. Auto-fix-eligibility: **needs-human-planning** — investigation-shaped, touches HAP/HomeSpan internals. Branch `fix/hap-heap-pressure-gate` is in flight with a working hap_heap_gate.patch; planning should account for the 528B intrinsic depth (sub-1Hz, below current gate resolution) and the corrected TASK_WDT diagnosis.

### [P1] log-audit-20260513-007 — force-close release callback runs after `clear_force_close_state` → counter=0 misattribution
**Status:** shipped in .80 (commit a37a0b3 + 6046884) and carried into .81/.82/.83; **soak validation still pending** — race window not yet exercised (5 sequences observed cumulatively, all non-race-path)
**Source:** log-audit 2026-05-13 (18h, 1440 lines); re-checked 2026-05-14 (3.5h, 281 lines); re-checked 2026-05-15 (24h, 1888 lines) on .82
**Issue:** (not filed)
**Acceptance:** force-close release callback short-circuits when `forceCloseInProgress` was cleared mid-hold; no `release sent (2-attempt mechanic, attempt 0/2)` log; the new `FORCE CLOSE: release sent, sequence already cleared (door began Closing during hold)` log line appears when the race fires; 7+ force-close sequences over 24h soak with no door-reverses-after-close-command behaviour.
**Notes:** P1 user-visible failure. Fix verified in source at comms.cpp:2813-2850 (entry-check on forceCloseInProgress + ESP_LOGI "sequence already cleared"). Device on .82 (status.json firmwareVersion="3.4.4-forceclose.82", uptime ~27.5h at 2026-05-15 audit end, crashCount=0). **5 HK-initiated FC sequences observed cumulatively across .80+.81+.82, ALL took the non-race path** (Closing-during-hold/TTCtimer.detach). New on this audit: FC#3 (2026-05-14 16:25:30, Closing concurrent with press), FC#4 (2026-05-14 18:48:08, Closing 1.37s into 2500ms hold), FC#5 (2026-05-14 20:00:12, Closing 1.40s into hold). The `sequence already cleared` race log line has **NEVER appeared** — the user's GDO consistently enters Closing within 1.0-1.5s, well inside the 2500ms hold, so the post-clear release-callback race window has not been naturally exercised. Code review confirms fix is correct; soak metric ("7+ sequences with no reverse") trivially met by the non-race path but the targeted race log condition (the actual acceptance criteria) remains untriggered. Auto-fix-eligibility: **shipped, awaiting telemetry** — consider re-classifying acceptance to "verified via static review" since natural race-trigger frequency may be measured in months. NOTE: finding 009's reentry symptom now covered separately by the .83 FC cooldown gate.


### [P3] codebase-audit-20260517-005 — extract homekit_health_update_adaptive_cadence helper (homekit_health_log grew to ~280 LoC)
**Status:** queued
**Source:** codebase-audit 2026-05-17 (.83+.84 file-size growth — homekit.cpp +228 LoC in 7 days)
**Issue:** (not filed)
**Acceptance:** in-callback adaptive cadence block (homekit.cpp:890-947, ~57 lines) extracted to homekit_health_update_adaptive_cadence(uint32_t freeHeapNow) static helper. homekit_health_log drops below 240 LoC again. Paired naming with homekit_health_arm_fast_mode_if_low so the entry/in-callback split is obvious.
**Notes:** S — single-file refactor, mechanical move into static helper. Counterpart to AUDIT-2026-05-17-002 if that fix uses the deferred-arm pattern (the extracted helper becomes the consumer of the pending-arm flag). Defer until 001/002 are resolved so the extraction can pick up the right pattern. Confidence high. Heap delta: 0 B. Auto-fix-eligibility: **auto-fixable**.

### [P3] code-review-20260519-001 — TWDT ISR diagnostic hook `esp_task_wdt_isr_user_handler` lacks `IRAM_ATTR`
**Status:** deferred (conditional — act only if a real TWDT fires and the diagnostic line is missing from output; not drain-actionable until then)
**Source:** code-review on PR for log-audit-20260518-001
**Issue:** (not filed)
**Acceptance:** add `IRAM_ATTR` to `esp_task_wdt_isr_user_handler` in `src/log.cpp` so the ISR body is IRAM-resident and cannot stall on a flash-cache lock. Clean `pio run -e ratgdo_esp32dev`.
**Notes:** XS — single-token addition. The TWDT ISR hook added for log-audit-20260518-001 is not `IRAM_ATTR`-decorated. Technically correct as-is — a TWDT is a software timer, flash cache is normally active when it fires, and IDF documents the user handler without requiring IRAM. But if the TWDT ever fires during a flash-cache stall, the function-body fetch from flash could hang and the `TWDT ISR: running CPU0=... CPU1=...` line would not appear. Marginal — only worth doing if a future TWDT event shows the diagnostic line missing. ESP32-only (function is already `#ifdef ESP32`-gated). Confidence high. Heap delta: 0 B. Auto-fix-eligibility: **auto-fixable**.

### [P3] code-review-20260519-002 — `setup_homekit()` boot-arm of `homekitHealthTicker` is not guarded by `healthTickerMux`
**Status:** deferred (defensive-only — guarded by the existing `homekit_setup_done` one-shot; no active bug)
**Source:** code-review on PR for codebase-audit-20260517-002
**Issue:** (not filed)
**Acceptance:** if `setup_homekit()`'s one-shot guard is ever weakened or removed, wrap the boot-time `detach()`/`attach_ms()` pair (`src/homekit.cpp` ~1884-1885) in `taskENTER_CRITICAL(&healthTickerMux)` / `taskEXIT_CRITICAL`. No action needed while the guard holds.
**Notes:** XS — the codebase-audit-20260517-002 spinlock fix deliberately leaves the boot-time Ticker arm unguarded because it runs single-threaded before the esp_timer task is live (correct today). Latent fragility only: correctness depends on `setup_homekit()` never executing twice, enforced by the `homekit_setup_done` one-shot. Filed so the dependency is explicit; not actionable while the guard stands. Confidence high. Heap delta: 0 B. Auto-fix-eligibility: **auto-fixable**.

---

## Recently completed (log-audit-20260518-001, 2026-05-19)

- **codebase-audit-20260517-003** (P3) — `heap_caps_get_largest_free_block` was called every 1Hz in the `service_timer_loop` hot path for an observability-only value (`maxBlock`) discarded 99.99% of the time. Now computed only when `freeHeap < HOMEKIT_HEALTH_HEAP_WATERMARK`, else `0` — behavior-identical because `homekit_health_arm_fast_mode_if_low` early-returns before reading `maxBlock` on the healthy path. `HOMEKIT_HEALTH_HEAP_WATERMARK` constexpr moved to `homekit.h` so `ratgdo.cpp` shares the constant (no magic-number dup). Drops the hot path from 2 heap-mutex reads/sec to 1. Build-verified clean; code-review APPROVED.
- **codebase-audit-20260517-002** (P3) — homekitHealthTicker cross-task re-arm race. The Arduino `Ticker` wrapper's `_timer` pointer write/free is not atomic, so concurrent `detach()`+`attach_ms()` from loopTask (`homekit_health_arm_fast_mode_if_low`) and the esp_timer task (in-callback adaptive block) could leak an `esp_timer` handle (~100 B) and double-register the callback. Fixed by guarding both detach+attach pairs with a `portMUX` spinlock (`healthTickerMux`), ESP32-only. The queue's preferred deferred-flag pattern was evaluated and rejected by the planner: deferring the loopTask arm to the next esp_timer callback would delay fast-mode entry up to 180s and defeat the heap-watermark trigger's immediate-arm purpose. Boot-time arm left unguarded (single-threaded). Build-verified clean; code-review APPROVED.
- **log-audit-20260518-003** (P2) — **closed: observability-only, no fix.** 30s-cadence 15-25KB heap dips every 6-10 min during iOS idle. The item's acceptance was "classify the allocator, resolves to no action if same allocator as -001/-002." Allocator classification is infeasible for the same reason -002 closed (heap-trace not linkable under `framework = arduino`; failed-alloc callback disqualified by the .86 storm). The dips never crash and recover in a single sample — same self-recovering heap-churn family as -001/-002, just shallower. Folded into log-audit-20260517-001. No firmware change.
- **log-audit-20260518-002** (P1) — **closed: observability-only, no fix.** Sub-1Hz heap transient (`min_free_heap_ever=528 B` on .85) — allocator identification proved infeasible. Both candidate instrumentation mechanisms are dead ends: (a) `heap_caps_register_failed_alloc_callback` is disqualified by log-audit-20260518-004 (the .86 callback caused a 25-fire reboot storm, reverted in .87); (b) heap-trace standalone is not linkable — the precompiled arduino-framework libs ship `CONFIG_HEAP_TRACING_OFF=1`, no `heap_trace.h` header, no `heap_trace_*` symbols; enabling it would require migrating the build from `framework = arduino` to `framework = espidf` (a build-system overhaul, out of scope for a diagnostic item). Per the item's own acceptance, confirmed the existing recovery path is sufficient: 10+ sub-1KB transients across .84/.85 with **zero OOM panics** — the 528B intrinsic watermark proves a dip the device survived. The allocator-identification objective is folded into log-audit-20260517-001 (parent investigation, .86 HAP preflight gate is the shipped read-side mitigation). No firmware change.
- **log-audit-20260518-001** (P0) — TASK_WDT reboot diagnostic capture. Investigation found `CONFIG_ESP_TASK_WDT_PANIC=1` is already enabled via the precompiled arduino-framework libs (`sdkconfig.defaults` is inert under `framework = arduino`), so the existing `panic_handler` + coredump path already captures a TWDT backtrace — the 2026-05-17 event's `/crashlog` was empty only because the following OTA wiped RTC_NOINIT. Added an `esp_task_wdt_isr_user_handler` weak-symbol override (`src/log.cpp`, ESP32-only) that runs in the TWDT ISR before the panic and `esp_rom_printf`s the task running on each core — naming the CPU0 hog, the one signal the coredump alone doesn't give (only IDLE0 is TWDT-subscribed). Plus a 2-line `sdkconfig.defaults` comment marking the inert TWDT keys (they had already caused one misdiagnosis). Build-verified clean; weak-symbol override linked with no `multiple definition` error. Acceptance is latent — verified on the next non-OTA-clobbered reset-reason-6 event.

## Recently completed (.85 cleanup, 2026-05-17, PR #135)

- **log-audit-20260517-002** (P2) — tickDrift -150000ms artifact after cadence transitions. Drift formula now reads currently-armed interval via `__atomic_load_n(&currentHealthIntervalMs, __ATOMIC_RELAXED)`; both re-arm sites zero `lastTickMs` so first post-transition sample reports `tickDrift=0`. Steady-state drift now matches pre-.84 baseline (~100ms). No functional impact (watchdog unaffected); telemetry hygiene only. **Acceptance MET 2026-05-18 audit**: 372/424 post-.85 samples = exactly 0ms, all others ±3ms; pre-.85 portion of same audit window had 721x -150000ms confirming the prior regression and the .85 fix exclusively addresses it.
- **codebase-audit-20260517-001** (P3) — `fastModeEntryMs` / `lastTickMs` int64 cross-task non-atomic. Both now `static volatile _millis_t` with `__atomic_store_n(RELEASE)` / `__atomic_load_n(ACQUIRE)` at all sites. Snapshot-once pattern at multi-check reads. Zero-byte change.

## Recently completed (.84 follow-up, 2026-05-17, PR #132)

- **log-audit-20260515-010 visibility gap** — .83's adaptive sampler missed the 3424B sub-180s dip because the trigger condition was only checked at slow-mode (180s) poll boundaries. .84 adds a 1Hz loopTask heap-watermark check via new `homekit_health_arm_fast_mode_if_low()` helper. Arms fast mode immediately on the drop, independent of poll cadence. Strictly additive — existing in-callback adaptive block continues to handle the stay-fast-5min-after-recovery logic. No FC / dedup / cooldown interactions.

## Recently completed (.83 bundle, 2026-05-16, PR #130 / commit 9dd52ea)

- **log-audit-20260515-010** (P1) — heap visibility: new ESP32-only `GET /heap` endpoint + adaptive HomeKit-health sampler cadence (180s → 30s when free heap < 20KB, holds 5min after recovery). 72h soak acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82, .83 NOT yet flashed)**: NEW deepest-dip-ever observed — heap=4000 maxBlock=2164 at 2026-05-15T19:58:26 CDT (uptime 167403s, ~46.5h). 4000B is 396B above the 4096B heap-floor short-circuit (yesterday's record was 4396, only 300B above). maxBlock=2164 means 54% of remaining heap fragmented into sub-2KB chunks — worst fragmentation snapshot ever recorded. Recovered to 42204 by next 180s sample (so dip lasted <3min). Single user activity in window: HK door open/close ~6 minutes prior (18:21:33 / 18:31:05 CDT) — heap had recovered to 42180 baseline by 19:55:26 then crashed 24KB at 19:58:26 with iOS quiet (last_hap_read_ago=899s, hintLevel=1). RSSI was -43dBm (worst observation in window, baseline -38dBm) suggesting lwIP rx-path or WiFi-stack consumption under marginal signal. SECOND DIP same window: heap=19320 maxBlock=21492 at 23:13:26 CDT, also 3-5min after door operations (Closing 23:03:06, Closing 23:07:50). Deploy of .83 with adaptive 30s cadence and `/heap` endpoint is the right next step — would have captured ~6 fast-samples bracketing the 4000-dip recovery curve. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed earlier today)**: NEW DEEPEST-DIP-EVER on .83 — heap=3424 maxBlock=1908 at 2026-05-16T11:16:18 CDT (uptime 12423s, ~3.4h post-OTA-boot). 3424B is 672B below prior 4000B record, only 672B above the 4096B heap-floor short-circuit. maxBlock=1908 = 56% sub-2KB fragmentation (worst ever, beats yesterday's 54%). last_hap_read_ago=33s (iOS active), RSSI=-37dBm baseline-normal — NOT correlated with marginal-signal hypothesis. No user activity within 30min prior. Recovered to 22064 (next 180s sample) then 39972 (sample after). **CRITICAL .83 FEATURE GAP CONFIRMED**: adaptive sampler 180s→30s acceleration line "HomeKit health sampler cadence: 180000ms -> 30000ms" was NEVER emitted for this dip — only the reverse 30000ms -> 180000ms line at 14:45:19 CDT (after the heap had been steady-state >40KB for 3.5h) appeared, meaning the gate WAS armed during the 11:16 dip but the dip occupied <180s and the next sample already showed 22064 (above the 20KB threshold), so cadence-acceleration ran for ≤1 cycle and the 180→30 announce never logged (or logged but the dip resolved before any 30s sample captured it). Net result: .83 adaptive sampler DOES NOT catch sub-3min transients — the very class of event we deployed it for. Need either (a) much higher poll rate (5-10s) when free heap < 30KB instead of < 20KB, or (b) interrupt-driven heap-low-watermark notification via FreeRTOS `heap_caps_register_failed_alloc_callback` style hook, or (c) sample-on-allocation-failure rather than time-poll. /heap endpoint not exercised in this window (would need on-demand fetch during a future dip — automate via the Pi).
- **log-audit-20260515-011** (P2) — SEC1 retry log enrichment: both first-of-burst and `[+N suppressed]` lines now include `[cmd=0xNN retries=N bus_silent=Nms cts=N rx_pending=N]` for root-cause attribution. 48h structured-log acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82)**: 2 more SEC1 TX retry events at 2026-05-15T23:02:52 and 23:07:37 CDT, both 1.0-1.5s before successful Open→Closing transitions (cmd transition timing identical to prior pattern). Pre-.83 log line still missing diagnostic suffix — confirms enrichment is needed; not a new pattern, just continued observation. Door closed cleanly each time (12047/12049ms). Cumulative SEC1-failure-then-success count now ~10 across .80/.81/.82 with zero functional impact. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed)**: 1 more SEC1 TX retry event at 2026-05-16T19:02:18 CDT (uptime 40386s, ~11.2h post-boot), enriched suffix `[cmd=0x39 retries=11 bus_silent=234ms cts=0 rx_pending=0]` — identical pattern to the 07:52:51 and 08:00:11 enriched events from earlier today's pre-audit window. Fired 1.5s before clean Open→Closing transition (12171ms close, no door reverse, no functional impact). Cumulative .83 enriched-SEC1 events now 3, all `cmd=0x39` (door state query) or `cmd=0x38`, all `retries=11 bus_silent~234ms cts=0 rx_pending=0` — pattern stable: GDO bus is silent for ~234ms during state-change moments, retries exhausted but actual command lands. Acceptance trending toward MET (need ~48h more enriched data).
- **log-audit-20260515-009** (P1) — HK Close-burst dedup + force-close reentry cooldown. New `hk_target_is_redundant()` helper at HK callback layer + `forceCloseClearedAtMs` stamp set in `clear_force_close_state`, gate in `door_command_force_close` (3s window). Strictly additive to existing FC safety gates (.80 release-callback fix, 2-attempt mechanic, CURR_CLOSED/CLOSING/0xFF entry gate, TAS). Code-review verified all 10 FC safety properties PASS.
- **log-audit-20260514-008** (P1) — HK Open-burst dedup: same `hk_target_is_redundant()` helper handles Open path. Mid-cycle reversals preserved.
- **tickDriftMs cast fix** (code-review out-of-scope finding from PR #130 review) — `src/homekit.cpp:688` time math fixed (was producing garbage int32-overflow values at ~25-day uptime).

## Deferred — soak-pending only

### [P3] HEAP-PRESSURE-WATCH — `mdns_networking: Cannot allocate memory` events under sustained operation
**Status:** deferred (`.79` heap-floor short-circuit makes the symptom non-lethal; OOM bursts no longer observed during overnight soak). Re-evaluate after .83 `/heap` + adaptive-cadence data identifies the transient allocator.
**Source:** 2026-05-09 .77 panic-loop investigation. Crash dump showed `free heap: 136 bytes` when mdns_mem_calloc failed. iOS hub state-sync + HomeSpan HAP + mDNS query bursts can momentarily consume nearly all heap. With `.79`'s fix (mdns task gated + 4KB heap floor) any such event is silently dropped at the syslog hook.
**Acceptance:** identify what's consuming heap during these bursts (heap-trace, periodic snapshots) and either bump tcpip MEMP pools / mdns rx-buf / cap HAP transaction concurrency, OR conclude the existing 30-50 KB headroom is enough now that the panic chain is fixed. Overnight `.79` soak shows heap stable at 38-42 KB — no OOM events in 10h+ — leaning toward "no further action needed" unless symptoms reappear.
**Notes:** panic chain is the priority — fixed at the syslog re-entry point so 100% heap exhaustion in mDNS no longer crashes. Investigate only if device experiences other symptoms (slow HAP, dropped notifications) under sustained low-heap conditions. Not user-visible right now.

---

## Upstream filing — DO NOT FILE

The fork's bug fixes (R1-R4 in `audit-notes/UPSTREAM_CHERRY_PICK_PLAN.md`) are already shipped in the fork. Their upstream-applicability is tracked in audit-notes/ for awareness only — **fork work proceeds regardless of upstream interest**.

**Hard rule:** No upstream PRs, no upstream issue filings, no cherry-picks to upstream. Issue #170 was filed historically; do not let it set precedent. Future agents reading this queue: if you find yourself drafting a `gh pr create --repo ratgdo/...`, STOP — that's not how this fork operates.

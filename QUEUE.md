# homekit-ratgdo32 work queue

Priority-ordered. Top = next. Detailed analysis lives in `audit-notes/` (gitignored fork-internal). Done items are removed — git log is the durable record.

## Active

### [P2] log-audit-20260522-001 — lwIP socket EAGAIN (`fail on fd 52, errno: 11 "No more processes"`) on .89 — 2nd event confirms benign static-fd noise (recommend re-close as P3)
**Status:** queued (sentinel-watch — **2nd occurrence on 2026-05-23 audit landed on the BENIGN branch of the acceptance: fd held STATIC at 52, zero functional impact → "= benign EAGAIN strerror noise" path, NOT the "fd climbs → escalate P1" path. Re-close-as-benign condition is now met; left queued for one user-confirmation pass per "change nothing, watch" mode. Do not change firmware.**)
**Source:** log-audit 2026-05-23 10:34Z (Pi syslog `/var/log/ratgdo.log` + `.1`, ~29.6h window, sorted chronologically; device endpoints status.json + /heap captured, /showlog + /showrebootlog + /crashlog Digest-gated/empty — `passwordRequired:true`). Prior: log-audit 2026-05-22 06:01Z.
**Issue:** (not filed — gh issue create denied by auto-mode classifier; queue-only per task scope; local issue body at /tmp/issue-body.md)
**Acceptance:** re-grep `fail on fd|No more processes` each audit. Close as benign (P3) if no 2nd event appears over a 7-day window, OR if a 2nd event shows a flat/random fd number with zero functional impact (= benign EAGAIN strerror noise). Escalate to P1 if fd numbers climb across events (socket-table leak signal) or any functional fault (pair loss / SSE slot leak / crash) correlates with an event.
**Notes:** P2 latent/observability — NO device fault underneath. **2026-05-23 10:34Z audit — 2nd occurrence, fd STATIC, benign branch satisfied:** `2026-05-22 10:46:35 CDT (2026-05-22T15:46:35Z) E ARDUINO: fail on fd 52, errno: 11, "No more processes"` — **SAME fd=52 as the first event (did NOT climb)**, ~12.4h after event #1. Two events now, both fd=52: the fd number is STATIC across occurrences, which is the explicit "benign EAGAIN strerror noise" signature in the acceptance (the socket-table-leak hypothesis would show fd CLIMBING — it did not). Zero functional impact: heap flat ~41.85K before (41852) AND after (41872), diag-sse all-zero before+after (sseSlotsAlloc=0/sseOrphansReaped=0 — no SSE-path fd leak), recoverAttempts=0, paired=yes/controllers=4, crashCount=0, uptime monotonic. **Identical trigger context to event #1**: both fired during an iOS-quiet L3 window (event #2 at hintLevel=3, last_hap_read_ago=2415s ~40min idle; event #1 ~5min after an L3 hint at ago=1953s) — confirms the EAGAIN is an Arduino-layer would-block during iOS reconnect after extended idle, not a leak. Recurrence: 2 over a 2-day window, fd static at 52. **Recommendation: re-close as benign P3 — the 2nd-event/static-fd/zero-impact criteria in the acceptance are all met.** Auto-fix-eligibility: **needs-human-planning** (re-close decision needs user sign-off per "change nothing, watch" mode); no firmware change warranted. PRIOR (2026-05-22 06:01Z): event #1 `2026-05-21 22:22:30 CDT E ARDUINO: fail on fd 52, errno: 11`; `errno 11` = EAGAIN (resource temporarily unavailable), the "No more processes" text is newlib's strerror for EAGAIN, not actual process-table exhaustion. Self-recovered, `HAP reads resumed` next sample.

### [P2] log-audit-20260521-001 — iOS-quiet watchdog L3 rate (peaked 1.09/h on .89; FALLING — 0.167/h trailing-6h at 2026-05-22 01:33Z cross-audit, below the 0.50/h reopen gate; ELIGIBLE TO RE-CLOSE as benign — left deferred per user)
**Status:** deferred (watch — user wants 2+ days of cross-audit data before any change. No device fault underneath: all L3 self-recovered, recoverAttempts=0. **Trend FALLING and now holding below the 0.50/h reopen gate over a clean trailing-6h denominator (0.167/h) with recoverAttempts=0 and zero pair loss — re-close condition is met; the item is eligible to re-close as benign. Left deferred only because the user is in "change nothing, watch a couple days" mode and wants the call.** .89 has cleared 24h soak. No device fault underneath.)
**Source:** log-audit 2026-05-21 22:35Z (Pi syslog `/var/log/ratgdo.log` + `.1` + `.2.gz`, sorted chronologically; device endpoints empty/Digest-gated)
**Issue:** (not filed — gh issue create denied by auto-mode classifier; queue-only per task scope)
**Acceptance:** re-measure sustained L3/h at next audit over a >=6h window. Re-close as benign if L3/h holds below 0.50 with continued recoverAttempts=0 / zero pair loss; OR tune `hkHintLikelyNRSecs` (currently 1800s) if the ~30-min iOS-idle band is accepted as normal for this hub. Escalates to P1 if ANY L3 fails to self-recover, recoverAttempts>0, or `HomeKit pair lost` appears.
**Notes:** P2 observability/latent — NO device fault underneath. **2026-05-23 10:34Z cross-audit (~29.6h window, 2026-05-22T00:00->2026-05-23T05:34 CDT; fresh-run baseline, no checkpoint state file): full-window L3 = 13 escalations over the 27.76h L3 span (00:51:48 -> 04:37:22 CDT) = 0.468/h (just below the 0.50/h gate), BUT trailing-6h denominator (23:34 -> 05:34 CDT) = 5 escalations = 0.833/h — ABOVE the 0.50/h reopen gate.** Item correctly STAYS open/deferred (does not re-close) this audit. Trajectory remains non-monotonic: ...0.415 -> 0.167 -> 0.667 -> **0.833**/h trailing-6h. All 13 L3 self-recovered (68 `recovering from level`/`HAP reads resumed` confirmations in window), recoverAttempts=0 across the entire window, zero `HomeKit pair lost`, zero confirmed-NR, controllers=4/paired=yes throughout, RSSI -33..-45dBm, crashCount=0, uptime monotonic 105299s->211759s (zero reboots, single continuous .89 boot ~58.8h per /heap uptime_s=211747). L1=69, L2=27, L3=13 this window. L3 depth band UNCHANGED: last_hap_read_ago=1808-1953s (~30-33min iOS idle) at every trigger — depth never changed, only frequency oscillates. Root cause remains hub-side iOS idle, NOT a firmware regression. The 0.833/h trailing-6h is driven by a tighter recent denominator; no functional fault underneath (recoverAttempts=0). **2026-05-22 06:01Z cross-audit — trailing-6h denominator (post-checkpoint window was ~4.4h): 19:01->01:01 CDT, L3 = 4 escalations = 0.667/h.** This is UP from the prior 0.167/h and now ABOVE the 0.50/h reopen gate — but driven by a wider, longer denominator that re-includes the 19:37 isolated escalation, and all 4 L3 still self-recovered (recoverAttempts=0, zero pair loss, controllers=4/paired=yes). Window itself (20:36->01:01 CDT, 4.4h): 3 L3 = 0.682/h (L1=10, L2=7, L3=3) at 22:17:25 (ago=1953s), 23:46:40 (ago=1830s), 00:51:48 (ago=1860s) — each followed by `HAP reads resumed`. Depth band unchanged (~30-33min iOS idle). The rate ticked back above the gate this audit, so the item correctly stays open/deferred rather than re-closing; trajectory now non-monotonic (...0.415 -> 0.167 -> 0.667). Still hub-side iOS idle, not a firmware regression. **2026-05-22 01:33Z cross-audit — trailing-6h denominator (the post-checkpoint window was only ~2.95h, too short, so a clean trailing 6h ending at the last syslog line was used per acceptance): 14:33->20:33 CDT, L3 = 1 escalation = 0.167/h sustained — DOWN from 0.415/h (-0.248/h), well below the 0.50/h reopen gate.** Post-checkpoint window itself (17:38->20:33 CDT, 2.95h): 1 L3 (L1=6, L2=2, L3=1). Full .89-boot rate (16 L3 over 25.8h) = 0.62/h but that folds in the early-boot cluster already counted; the recent trajectory is what matters. **Striking gap in the L3 timeline confirms the fall**: a dense early cluster ran 2026-05-20 20:45 -> 2026-05-21 13:48 CDT (15 escalations), then a ~5.8h SILENCE with ZERO L3, then a single isolated L3 at 19:37:07 CDT (last_hap_read_ago=1804s, recovered next read "recovering from level 3" at 20:12:06). recoverAttempts=0 across all 130 diag-hk samples this window (and zero across the full .89 boot), zero pair loss, zero confirmed-NR, controllers=4 / paired=yes throughout, RSSI -41..-47dBm. L3 depth band unchanged: last_hap_read_ago 1804-1960s (~30-33min) at every trigger — only the depth never changed, frequency has reverted to (and now below) the long-run band. Root cause remains hub-side iOS idle, not a firmware regression. Trajectory: 0.62 -> 0.42 -> 0.28 -> 0.30 -> 0.00 -> 0.00 -> 0.40 -> 0.179 -> 0.408 -> 1.087 -> 0.415 -> **0.167**/h. **The 1.087/h spike that reopened this item is now two audits behind; the rate has stayed below the reopen gate for two consecutive cross-audits (0.415 then 0.167). Re-close condition met — recommend re-close as benign at user's discretion.** **2026-05-21 22:35Z cross-audit (12.05h window, 10:32Z->22:35Z): L3 = 5 escalations = 0.415/h sustained** — DOWN from the prior window's 1.087/h (-0.672/h), and below the 0.50/h reopen gate. Full .89-boot rate (15 L3 over ~22.9h) = 0.656/h. Trajectory: 0.62 -> 0.42 -> 0.28 -> 0.30 -> 0.00 -> 0.00 -> 0.40 -> 0.179 -> 0.408 -> 1.087 -> **0.415**/h. The 1.087/h spike that reopened this item was a window artifact; the rate has reverted to the long-run band. All 5 L3 self-recovered (`HAP reads resumed, recovering from level 3`), `recoverAttempts=0` across all 545 diag-hk samples, zero pair loss, zero confirmed-NR, controllers=4 / paired=yes throughout. L1=32, L2=15, L3=5 this window. L3 depth band stable: last_hap_read_ago 1805-1960s (~30-33min) at trigger — only the depth never changed, frequency now reverting. Root cause remains hub-side iOS idle, not a firmware regression. Evidence (representative): `2026-05-21 13:05:55 W iOS silent — likely No Response (last_hap_read_ago=1960, threshold-level=3)` then resume `recovering from level 3` next read. Full L3 syslog timestamps (CDT): 08:45:46, 10:02:25, 11:05:34, 13:05:55, 13:48:45 (each followed by a resume). PRIOR (2026-05-21 10:32Z): 9 L3 in 8.28h = 1.087/h, the spike that reopened this. Auto-fix-eligibility: **needs-human-planning** — threshold-tuning decision needs user sign-off, but trend is self-resolving; leaning toward re-close. Local issue body retained at /tmp/issue-body.md.

### [P1] log-audit-20260517-001 — sub-1KB heap transient (free=864 maxBlock=372 → updated to 528B intrinsic on .85) — underlying allocator burst
**Status:** shipped in .86 (PR #137) — HAP preflight gate + failed-alloc safety net; soak validation pending
**Source:** log-audit 2026-05-17 05:00 (5h post-.84 boot, 180 lines), recurring + deeper in log-audit-2026-05-18 (24h window: 4 sub-2KB events, deepest free=800 maxBlock=372; plus `min_free_heap_ever=528 B` from `/heap` IDF intrinsic — see log-audit-20260518-002)
**Issue:** (not filed)
**Acceptance:** identify what's consuming heap during these sub-second transients OR confirm the existing recovery path (panic-loop-free for 7+ events) is sufficient and downgrade to observability-only. 10+ sub-1KB transients now observed across .84/.85 with only ONE crash, and that crash was TASK_WDT (timing) not OOM (allocator) — strengthens "recovery path is sufficient" path. **2026-05-19: instrumentation paths are exhausted** — heap-trace is infeasible (precompiled arduino-framework libs ship `CONFIG_HEAP_TRACING_OFF=1`, no `heap_trace.h`, no linkable `heap_trace_*` symbols; unlocking it needs an espidf-framework migration, out of scope) and `heap_caps_register_failed_alloc_callback` is disqualified by log-audit-20260518-004 (the .86 reboot storm). Path (b) "confirm recovery sufficient" is therefore the only live path; the .86 HAP preflight gate is the read-side mitigation already shipped.
**Notes:** P1 — `.84/.85`'s 1Hz heap-watermark check WORKS (catches the dip, fast-cadence arms, recovers within 27-30s, no OOM panic). **2026-05-22 06:01Z update — recurrence, recovery proven again, NO new all-time-deepest (window shallow), heap flat (no leak)**: across the ~4.4h window (uptime 93055s -> 108967s) health-sample heap stayed FLAT at ~42K baseline (first 41996, last 43252, maxBlock=38900 throughout) — zero sustained downward drift, no leak. ~2 fast-cadence dip cycles: deepest 1Hz-sampled health = **free=8680 maxBlock=17396 at uptime 95474s (21:16 CDT)**, recovered to baseline next sample; second dip free=11704 (27:08) and free=14516 (30:15) both drove clean 180000->30000ms cadence transitions and recovered to ~41.6K. None came near the .89-boot deepest pair (free=908, free=1720, both already credited). **`/heap min_free_heap_ever=132` UNCHANGED** (device endpoint, 2026-05-22 06:01Z) — 132B floor holds, no new all-time-deepest. diag-sse telemetry all-zero (sseBufferFullSkips/sseSlowWrites/sseSlotsAlloc/sseOrphansReaped=0 across all samples), logMtxMaxWait peak 2ms, recoverAttempts=0. ZERO failed-alloc fires (.87 revert holding, cross-audit #12). Recurrence, not new — recovery path proven sufficient again. **2026-05-22 01:33Z update — recurrence, recovery proven again, NO new all-time-deepest (window shallow)**: 16 heap-watermark/fast-cadence triggers in the 2.95h window. Deepest 1Hz-sampled health = **free=12064 maxBlock=12788 at uptime 88445s (19:13 CDT)** — shallow vs the established envelope (~12KB above the all-time floor), recovered to baseline next sample. No dip this window came near the .89-boot deepest pair (free=908 maxBlock=3060 at uptime 51491s and free=1720 maxBlock=10228 at uptime 75205s, BOTH already credited in the 22:35Z audit — not new). **`/heap min_free_heap_ever=132` UNCHANGED** (device endpoint, 2026-05-22 01:33Z) — no new all-time-deepest IDF intrinsic; the 132B floor holds. Device survived every dip — no OOM, no crash, recoverAttempts=0 across all 130 diag-hk. ZERO failed-alloc fires (.87 revert holding, cross-audit #11). diag-sse telemetry all-zero (sseBufferFullSkips/sseSlowWrites/sseSlotsAlloc/sseOrphansReaped=0), logMtxMaxWait peak 1ms. Recurrence, not new — recovery path proven sufficient again. **2026-05-21 22:35Z update — recurrence, recovery proven again, NO new all-time-deepest**: 31 heap-watermark triggers in the 12.05h window = 2.57/h (vs prior 2.29/h, comparable). Deepest 1Hz-sampled health = **free=908 maxBlock=3060 at uptime 51491s (14:18:15 CDT)**, recovered to free=41988 maxBlock=38900 in the VERY NEXT 30s fast-cadence sample (uptime 51521s) — same self-recovering allocator-class signature. Second-deepest free=1720 maxBlock=10228 at uptime 75205s, recovered to 41992 next slow sample. Deepest watermark-trigger free=3048 maxBlock=236 (07:23:36 CDT) — this one drove the new guard fire (see log-audit-20260520-001). **`/heap min_free_heap_ever=132` UNCHANGED — no new all-time-deepest IDF intrinsic this window** (the 132B floor from prior audit holds). Device survived every dip — no OOM, no crash, recoverAttempts=0 across all 545 diag-hk. ZERO failed-alloc fires (.87 revert holding, cross-audit #10). Recurrence, not new — recovery path proven sufficient again. **2026-05-21 10:32Z update — recurrence, recovery proven again, NEW deepest intrinsic**: 19 heap-watermark triggers in the 8.28h window = 2.29/h. Deepest 1Hz-sampled = **free=1840 maxBlock=2420 at 01:01:44 CDT** (uptime ~22593s), recovered to free=43612 maxBlock=38900 in the VERY NEXT 30s fast-cadence sample (01:02:14) — same self-recovering allocator-class signature, ~1572B above the all-time 1Hz floor (268B). NEW all-time-deepest IDF intrinsic: `/heap min_free_heap_ever=132` this boot (68B below prior all-time 200B record; sub-1Hz, same sub-Hz transient family folded in under log-audit-20260518-002). Device survived it — no OOM, no crash, recoverAttempts=0. ZERO failed-alloc fires (.87 revert holding, cross-audit #9). Recurrence, not new. **2026-05-21 00:21Z update — first post-.89 data**: 3 heap-watermark triggers in ~35min post-OTA-boot on .89: free=14464 maxBlock=9204 at 18:46:42 CDT (1.5min post-boot, expected during HK reattach), free=17420 maxBlock=14324 at 18:55:57 CDT, free=4700 (deepest this window, at 19:11:27 CDT, uptime ~26min). All recovered within ONE 30s fast-cadence cycle to heap ~40K, maxBlock ~34804. The free=4700 sample is sub-5KB but ~4432B above the all-time-deepest free=268 — still within established envelope. The new .89 `HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256` guard was NOT exercised this cycle (largest_free at every transition stayed well above 256B, so no `skipping cadence re-arm (largest_free=NB < 256B)` line). Cross-task spinlock + arm-failure detection both silent (healthy baseline). Heap-watermark rate = 3 in 35min = 5.1/h — appears elevated vs prior ~1.25-1.80/h, but post-boot HK-reattach pressure is normal in the first ~30min; needs longer post-OTA sample. Allocator-class signature unchanged. **2026-05-20 16:39Z update — second-deepest event recurrence**: `free=968 maxBlock=628` at 06:31:30 CDT (11:31:30Z), uptime 86001s, ~24h into same .88 boot. 968B is 700B above the all-time-deepest 268B record from prior audit, but still sub-1KB. Recovery one-cycle within 30s fast-cadence to free=41164. Same audit window also captured 6 other heap-watermark triggers: free=5340, 13508, 15796, 18012, 7952, 10328 (deepest = 5340/maxBlock=4852, second-deepest = 7952/maxBlock=7156). 7 heap-watermark triggers in 5.60h = 1.25/h sustained — UP from prior audit's 9 in 5.01h (1.80/h) so slight cadence drop. ZERO failed-alloc fires (.87 revert holding — cross-audit #7, 7 consecutive zero-fire audits). ZERO crashes, ZERO recoverAttempts, no HAP-gate denial, no door activity within 30min prior, RSSI -34 to -38dBm. Allocator-class behavior continues to match — sub-3-min self-recovering dips, recovery path proven sufficient. **2026-05-20 11:02Z prior dip**: `free=268 maxBlock=16` at 03:33:42 CDT, uptime 75322s, still the all-time deepest record. 268B is 3828B below prior record (free=2652 → 528B intrinsic / free=800 1Hz-sampled). maxBlock=16 means heap is functionally exhausted (any allocation >16B would fail). The dip self-recovered within ONE 30s fast-cadence cycle to free=41716 maxBlock=23540 — same allocator-class signature, just much deeper. ZERO failed-alloc callback fires (.87 revert holding), ZERO recoverAttempts, ZERO crashes, no HAP gate denial, no door activity within 30min prior, RSSI -39dBm baseline. iOS was quiet (hkHealthyTicks=22, hintLevel=0 at the moment of trigger; L1 hint logged 30s later as "iOS extended idle ago=329s threshold-level=1"). 9 heap-watermark triggers total this window (free values: 19708, 8020, 16100, **268**, 18624, 18888, 18240, **2652**, 15616). Two sub-4KB events (free=268 and free=2652) this window vs zero in prior audit and three in audit before that — sub-4KB rate stays within established envelope. Allocator class behavior identical, just exploring deeper magnitudes. Recovery path is sufficient as established. The 528B IDF intrinsic (logged at /heap on .85) is no longer the floor — 1Hz polling now confirms a 268B floor exists in real time. **2026-05-18 update — diagnosis correction**: the prior status header attributed the 2026-05-17T08:20 reboot to "HomeSpan HAP TempBuffer ctor at free=324B" — that was conjecture. The actual `esp_reset_reason()` value was 6 = ESP_RST_TASK_WDT (task watchdog timeout), not 4 = ESP_RST_PANIC (allocator abort) or 9 = ESP_RST_BROWNOUT. Pre-crash heap was 42268B (healthy) at 08:19:53, with reboot 18s later at 08:20:11. The TASK_WDT class of crash is now tracked as a peer P0 finding (log-audit-20260518-001) — heap pressure may be a contributing factor (lwIP/WiFi spin under transient OOM cascading into TWDT) but the root cause is task-blocked, not allocation-failed. **2026-05-18 deepest-event update**: free=800 maxBlock=372 at 02:24:49 CDT (.84, 64B below the prior 864B record), free=804 at 04:17:55 CDT, free=1580 at 09:59:25 CDT, free=1616 at 09:28:06 CDT — 4 sub-2KB events in 24h. Plus the IDF intrinsic `min_free_heap_ever=528 B` from /heap on .85 (sub-Hz dip below the 1Hz polling resolution — separate evidence tracked as log-audit-20260518-002). RSSI consistently -34 to -39dBm (good), no concurrent door activity, no SEC1 retry preceding. Same allocator likely also responsible for the 30s-cadence 15-25KB shallow dips observed on .85 (log-audit-20260518-003). Suspect: HomeKit pairing-verify, HAP-event-bulk-notify (4 controllers paired), TLS/curve25519 transient, or mDNS query burst. Recovery proves 1Hz check is the right safety net but doesn't identify the allocator. Auto-fix-eligibility: **needs-human-planning** — investigation-shaped, touches HAP/HomeSpan internals. Branch `fix/hap-heap-pressure-gate` is in flight with a working hap_heap_gate.patch; planning should account for the 528B intrinsic depth (sub-1Hz, below current gate resolution) and the corrected TASK_WDT diagnosis.

### [P1] log-audit-20260513-007 — force-close release callback runs after `clear_force_close_state` → counter=0 misattribution
**Status:** shipped in .80 (commit a37a0b3 + 6046884) and carried into .81/.82/.83; **soak validation still pending** — race window not yet exercised (5 sequences observed cumulatively, all non-race-path)
**Source:** log-audit 2026-05-13 (18h, 1440 lines); re-checked 2026-05-14 (3.5h, 281 lines); re-checked 2026-05-15 (24h, 1888 lines) on .82
**Issue:** (not filed)
**Acceptance:** force-close release callback short-circuits when `forceCloseInProgress` was cleared mid-hold; no `release sent (2-attempt mechanic, attempt 0/2)` log; the new `FORCE CLOSE: release sent, sequence already cleared (door began Closing during hold)` log line appears when the race fires; 7+ force-close sequences over 24h soak with no door-reverses-after-close-command behaviour.
**Notes:** P1 user-visible failure. Fix verified in source at comms.cpp:2813-2850 (entry-check on forceCloseInProgress + ESP_LOGI "sequence already cleared"). Device on .82 (status.json firmwareVersion="3.4.4-forceclose.82", uptime ~27.5h at 2026-05-15 audit end, crashCount=0). **5 HK-initiated FC sequences observed cumulatively across .80+.81+.82, ALL took the non-race path** (Closing-during-hold/TTCtimer.detach). New on this audit: FC#3 (2026-05-14 16:25:30, Closing concurrent with press), FC#4 (2026-05-14 18:48:08, Closing 1.37s into 2500ms hold), FC#5 (2026-05-14 20:00:12, Closing 1.40s into hold). The `sequence already cleared` race log line has **NEVER appeared** — the user's GDO consistently enters Closing within 1.0-1.5s, well inside the 2500ms hold, so the post-clear release-callback race window has not been naturally exercised. Code review confirms fix is correct; soak metric ("7+ sequences with no reverse") trivially met by the non-race path but the targeted race log condition (the actual acceptance criteria) remains untriggered. Auto-fix-eligibility: **shipped, awaiting telemetry** — consider re-classifying acceptance to "verified via static review" since natural race-trigger frequency may be measured in months. NOTE: finding 009's reentry symptom now covered separately by the .83 FC cooldown gate.


### [P3] code-review-20260519-001 — TWDT ISR diagnostic hook `esp_task_wdt_isr_user_handler` lacks `IRAM_ATTR`
**Status:** deferred (conditional — act only if a real TWDT fires and the diagnostic line is missing from output; not drain-actionable until then)
**Source:** code-review on PR for log-audit-20260518-001
**Issue:** (not filed)
**Acceptance:** add `IRAM_ATTR` to `esp_task_wdt_isr_user_handler` in `src/log.cpp` so the ISR body is IRAM-resident and cannot stall on a flash-cache lock. Clean `pio run -e ratgdo_esp32dev`.
**Notes:** XS — single-token addition. The TWDT ISR hook added for log-audit-20260518-001 is not `IRAM_ATTR`-decorated. Technically correct as-is — a TWDT is a software timer, flash cache is normally active when it fires, and IDF documents the user handler without requiring IRAM. But if the TWDT ever fires during a flash-cache stall, the function-body fetch from flash could hang and the `TWDT ISR: running CPU0=... CPU1=...` line would not appear. Marginal — only worth doing if a future TWDT event shows the diagnostic line missing. ESP32-only (function is already `#ifdef ESP32`-gated). Confidence high. Heap delta: 0 B. Auto-fix-eligibility: **auto-fixable**.

### [P3] code-review-20260519-002 — `setup_homekit()` boot-arm of `homekitHealthTicker` is not guarded by `healthTickerMux`
**Status:** deferred (defensive-only — guarded by the existing `homekit_setup_done` one-shot; no active bug)
**Source:** code-review on PR for codebase-audit-20260517-002
**Issue:** (not filed)
**Acceptance:** if `setup_homekit()`'s one-shot guard is ever weakened or removed, wrap the boot-time `detach()`/`attach_ms()` pair (`src/homekit.cpp` ~1884-1885) in `taskENTER_CRITICAL(&healthTickerMux)` / `taskEXIT_CRITICAL`. No action needed while the guard holds.
**Notes:** XS — the codebase-audit-20260517-002 spinlock fix deliberately leaves the boot-time Ticker arm unguarded because it runs single-threaded before the esp_timer task is live (correct today). Latent fragility only: correctness depends on `setup_homekit()` never executing twice, enforced by the `homekit_setup_done` one-shot. Filed so the dependency is explicit; not actionable while the guard stands. Confidence high. Heap delta: 0 B. Auto-fix-eligibility: **auto-fixable**.

---

## Recently completed (closure pass, 2026-05-23)

- **log-audit-20260518-004** (P0) — **closed: revert held, acceptance far exceeded.** The .87 revert (PR #138) removed the `heap_caps_register_failed_alloc_callback` that caused the 25-fire post-OTA reboot storm; the HAP heap-pressure gate (read-side observability) was retained. 72h soak target MET and far exceeded — across the full May 19→23 syslog span (8420 lines, ~4.25 days) there were ZERO `failed-alloc callback fired` lines, ZERO Guru/panic/task_wdt/brownout resets, and exactly ONE reboot (the planned .89 OTA, uptime reset 82565s→183s). .89 alone has run ~59h continuous (crashCount=0, paired/4 controllers). Storm regression is dead. No further firmware change.

## Recently completed (log-audit-20260520, 2026-05-20)

- **log-audit-20260519-001** (P2) — **closed: trend resolved, healthy iOS-idle quiet behavior, no device fault.** Cross-audit #7 (log-audit 2026-05-20 16:39Z, the prior cycle): **L3=0.179/h** crossed the 0.20/h close-watch gate from the acceptance criteria. Full trajectory across 7 audits on .87/.88: L3 rate 0.62 → 0.42 → 0.28 → 0.30 → 0.00 → 0.00 → 0.40 → **0.179**/h. All escalations self-recovered with recoverAttempts=0 — zero `HomeKit pair lost`, zero confirmed-NR, zero mDNS/HAP failure mode underneath. Behavior is iOS home-hub-side idle, not a device fault; the watchdog hint text itself anticipated this ("could be normal hub idle, watch the trend"). The instrumentation correctly led the data and the data resolved to "no action needed." Watchdog telemetry has since stopped emitting (see log-audit-20260520-001 below) but that is a separate observability defect on the producer side, not an L3 escalation. No firmware change.

## Recently completed (log-audit-20260520-001, 2026-05-20)

- **log-audit-20260520-001** (P1) — **ACCEPTANCE MET 2026-05-22 01:33Z** — `homekitHealthTicker` callback stopped firing for 6h+ on .88 after a heap-pressure cadence transition. Root cause: the Arduino `Ticker::_attach_us()` calls `esp_timer_create()` without checking the return — under heap pressure it returns `ESP_ERR_NO_MEM`, leaves `_timer = NULL`, and subsequent `esp_timer_start_periodic(NULL, ...)` silently fails. PR #140's spinlock made detach+attach atomic across tasks but didn't verify success. Fix in three layers: (1) **heap-pressure guard** (`HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256`) — if largest free block is below 256B, skip the cadence change entirely and let the existing periodic timer keep firing at its prior interval (cadence-state rolled back to match); (2) **arm-failure detection** via `Ticker::active()` check under `healthTickerMux`, stamped to a `volatile bool homekitHealthTicker_armFailed` flag + millis timestamp; (3) **deferred-retry path** on loopTask 1Hz — re-arms once heap recovers, with the warning log rate-limited to once/60s to avoid syslog flooding. Plus three new `/heap` JSON fields (`ticker_active`, `ticker_arm_count`, `ticker_arm_failed_at_ms`) for post-deployment observability without a reboot. Build-verified clean; code-review two-pass APPROVED (first pass flagged a 1Hz log-storm defect, fixed and confirmed in re-review). Acceptance is latent — verified by 24h+ continuous boot with no health-sample silence gaps > 2× current cadence, plus the new /heap fields populating sanely. **2026-05-21 00:21Z audit (35min post-.89-OTA)**: producer REVIVED on .89 — health/diag-sse/diag-hk samples streaming every 30s/180s as expected, vs zero HK lines on .88 from 14:29Z onward. /heap LIVE: `ticker_active:1`, `ticker_arm_count:7`, `ticker_arm_failed_at_ms:0` (clean baseline). 3 heap-watermark triggers post-boot (free=14464, 17420, 4700) all transitioned cleanly through the new guard — largest_free never dipped below 256B threshold during a transition attempt, so `skipping cadence re-arm` line has not appeared yet (the guard is reactive only). ZERO `Ticker arm FAILED` ESP_LOGE. 1 L1 watchdog hint (iOS idle ago=314s, recovered within 5min — base rate, not escalation). 1 cosmetic SSE orphan reap (browser TCP close, P3 noise). Acceptance trajectory positive at +35min; 24h soak target ~2.4% complete. Continue cycle; revisit at next audit. **2026-05-21 10:32Z audit (~10.8h into .89 boot)**: producer continuity STRONG — 337 HomeKit-health + 319 diag-sse + 319 diag-hk samples across the 8.28h window, **max inter-sample silence gap = 360s (exactly 2x the 180s slow cadence, AT the boundary, NOT > 2x — one coalesced/dropped slow tick, uptime monotonic across it so no producer stall)**, all other gaps <= 209s. NO violation of the "no gap > 360s" acceptance. /heap LIVE: `ticker_active:1`, `ticker_arm_count:48` (climbing from 11/7 prior, healthy), `ticker_arm_failed_at_ms:0`. ZERO `Ticker arm FAILED` ESP_LOGE, ZERO `skipping cadence re-arm (largest_free<256B)` guard fires (largest_free stayed above 256B at every transition — guard remains reactive-only, not exercised). 19 clean cadence transitions, 18 fast->slow announces all at free_heap ~41.6K. 24h soak ~44% complete (~10.8h continuous .89 boot). Trajectory strongly positive; if .89 clears 24h continuous with no >360s gap, acceptance is MET — on track. **2026-05-21 22:35Z audit (~22.9h into .89 boot)**: producer continuity STRONG and IMPROVED — 545 HomeKit-health + 545 diag-sse + 545 diag-hk samples across the 12.05h window, **max inter-sample uptime gap = 205s (well UNDER the 360s = 2x180s threshold; zero gaps > 209s, uptime monotonic, 0 out-of-order)** — strictly better than the prior window's single 360s borderline gap. /heap LIVE: `ticker_active:1`, `ticker_arm_count:113` (climbing from 48, +65 arms over ~12h, healthy), `ticker_arm_failed_at_ms:0`. ZERO `Ticker arm FAILED` ESP_LOGE. **NEW — the `HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256` guard FIRED for the first time and worked as designed**: `2026-05-21 07:23:36 W HomeKit health: skipping fast-cadence re-arm (largest_free=236B < 256B), staying at slow interval` — largest_free dipped to 236B (below 256B), the guard correctly skipped the cadence re-arm (which would have hit `esp_timer_create` ESP_ERR_NO_MEM), the existing periodic timer kept firing, and the producer never stalled. This is the exact protective path designed in the fix, now empirically validated under real heap pressure (not just reactive/untested). **24h soak ~95% complete (~22.9h continuous .89 boot, uptime_s=82295 at audit, crashCount=0)** — has NOT yet cleared the full 24h; one more audit (~1.1h+) confirms MET. Trajectory: acceptance imminent, guard validated. **2026-05-22 01:33Z audit (~25.81h into .89 boot — ACCEPTANCE MET): the .89 boot has now cleared the full 24h continuous with NO health-sample silence gap exceeding the 360s = 2x180s threshold.** Window (17:38->20:33 CDT, ~2.95h, 424 syslog lines): 137 health + 130 diag-sse + 130 diag-hk samples, **max inter-sample uptime gap = 210s** (well under 360s, zero gaps >210s, uptime monotonic, 0 out-of-order). Full-.89-boot peak inter-sample gap remains the single 360s borderline tick from the 10:32Z window (AT boundary, not >). /heap LIVE (device endpoint, serverTime 1779413625 = 2026-05-22T01:33:45Z, uptime_s=92914 = exactly 25.81h since OTA boot 1779320711 = 2026-05-20T23:45:11Z, confirmed continuous): `ticker_active:1`, `ticker_arm_count:131` (climbing from 113, +18 arms over ~3h, healthy linear growth), `ticker_arm_failed_at_ms:0`, crashCount=0. ZERO `Ticker arm FAILED` ESP_LOGE this window and across the full .89 boot. ZERO new `skipping fast-cadence re-arm (largest_free<256B)` guard fires this window — the single guard fire at 07:23:36 CDT (largest_free=236B, already credited in the 22:35Z audit) remains the lone empirical validation; the protective path is proven and not re-triggered. 9 clean fast->slow cadence transitions this window, all announced at free_heap ~41.6K. Producer continuity has now held strong across the entire 24h+ soak with the heap-pressure guard validated under real pressure. **Acceptance criteria fully satisfied — finalized/closed 2026-05-23 per user sign-off (guard validated under real heap pressure, ~59h continuous .89 soak, zero ticker-arm failures).** **2026-05-22 06:01Z audit (~30.3h into .89 boot) — acceptance continues to hold**: /heap LIVE `ticker_active:1`, `ticker_arm_count:156` (climbing from 131, +25 over ~4.4h, healthy linear growth), `ticker_arm_failed_at_ms:0`, crashCount=0. ZERO `Ticker arm FAILED` ESP_LOGE, ZERO new `skipping fast-cadence re-arm` guard fires (the 07:23:36 CDT largest_free=236B fire remains the lone empirical validation). Window producer continuity intact: 218 health samples across 4.4h, cadence transitions all clean at free_heap ~41.6K. Soak now 30.3h continuous — well past the 24h target.

## Recently completed (log-audit-20260518-001, 2026-05-19)

- **codebase-audit-20260517-005** (P3) — extracted the in-callback adaptive-cadence block of `homekit_health_log` (slow/fast cadence decision, 5-min fast-hold, cadence log line, spinlock-guarded Ticker re-arm) into a new `static void homekit_health_update_adaptive_cadence(uint32_t freeHeapNow)` helper, paired-named with `homekit_health_arm_fast_mode_if_low`. Pure mechanical extraction — `freeHeapNow` is the only parameter crossing the boundary; all other state is file-scope. `homekit_health_log` dropped from ~324 to 257 raw lines. The earlier "wait for the deferred-arm pattern" coupling is moot — codebase-audit-20260517-002 shipped a spinlock, not a deferred flag. Build-verified clean; code-review APPROVED, zero behavior change confirmed.
- **codebase-audit-20260517-003** (P3) — `heap_caps_get_largest_free_block` was called every 1Hz in the `service_timer_loop` hot path for an observability-only value (`maxBlock`) discarded 99.99% of the time. Now computed only when `freeHeap < HOMEKIT_HEALTH_HEAP_WATERMARK`, else `0` — behavior-identical because `homekit_health_arm_fast_mode_if_low` early-returns before reading `maxBlock` on the healthy path. `HOMEKIT_HEALTH_HEAP_WATERMARK` constexpr moved to `homekit.h` so `ratgdo.cpp` shares the constant (no magic-number dup). Drops the hot path from 2 heap-mutex reads/sec to 1. Build-verified clean; code-review APPROVED.
- **codebase-audit-20260517-002** (P3) — homekitHealthTicker cross-task re-arm race. The Arduino `Ticker` wrapper's `_timer` pointer write/free is not atomic, so concurrent `detach()`+`attach_ms()` from loopTask (`homekit_health_arm_fast_mode_if_low`) and the esp_timer task (in-callback adaptive block) could leak an `esp_timer` handle (~100 B) and double-register the callback. Fixed by guarding both detach+attach pairs with a `portMUX` spinlock (`healthTickerMux`), ESP32-only. The queue's preferred deferred-flag pattern was evaluated and rejected by the planner: deferring the loopTask arm to the next esp_timer callback would delay fast-mode entry up to 180s and defeat the heap-watermark trigger's immediate-arm purpose. Boot-time arm left unguarded (single-threaded). Build-verified clean; code-review APPROVED.
- **log-audit-20260518-003** (P2) — **closed: observability-only, no fix.** 30s-cadence 15-25KB heap dips every 6-10 min during iOS idle. The item's acceptance was "classify the allocator, resolves to no action if same allocator as -001/-002." Allocator classification is infeasible for the same reason -002 closed (heap-trace not linkable under `framework = arduino`; failed-alloc callback disqualified by the .86 storm). The dips never crash and recover in a single sample — same self-recovering heap-churn family as -001/-002, just shallower. Folded into log-audit-20260517-001. No firmware change.
- **log-audit-20260518-002** (P1) — **closed: observability-only, no fix.** Sub-1Hz heap transient (`min_free_heap_ever=528 B` on .85) — allocator identification proved infeasible. Both candidate instrumentation mechanisms are dead ends: (a) `heap_caps_register_failed_alloc_callback` is disqualified by log-audit-20260518-004 (the .86 callback caused a 25-fire reboot storm, reverted in .87); (b) heap-trace standalone is not linkable — the precompiled arduino-framework libs ship `CONFIG_HEAP_TRACING_OFF=1`, no `heap_trace.h` header, no `heap_trace_*` symbols; enabling it would require migrating the build from `framework = arduino` to `framework = espidf` (a build-system overhaul, out of scope for a diagnostic item). Per the item's own acceptance, confirmed the existing recovery path is sufficient: 10+ sub-1KB transients across .84/.85 with **zero OOM panics** — the 528B intrinsic watermark proves a dip the device survived. The allocator-identification objective is folded into log-audit-20260517-001 (parent investigation, .86 HAP preflight gate is the shipped read-side mitigation). No firmware change.
- **log-audit-20260518-001** (P0) — TASK_WDT reboot diagnostic capture. Investigation found `CONFIG_ESP_TASK_WDT_PANIC=1` is already enabled via the precompiled arduino-framework libs (`sdkconfig.defaults` is inert under `framework = arduino`), so the existing `panic_handler` + coredump path already captures a TWDT backtrace — the 2026-05-17 event's `/crashlog` was empty only because the following OTA wiped RTC_NOINIT. Added an `esp_task_wdt_isr_user_handler` weak-symbol override (`src/log.cpp`, ESP32-only) that runs in the TWDT ISR before the panic and `esp_rom_printf`s the task running on each core — naming the CPU0 hog, the one signal the coredump alone doesn't give (only IDLE0 is TWDT-subscribed). Plus a 2-line `sdkconfig.defaults` comment marking the inert TWDT keys (they had already caused one misdiagnosis). Build-verified clean; weak-symbol override linked with no `multiple definition` error. Acceptance is latent — verified on the next non-OTA-clobbered reset-reason-6 event.

## Recently completed (.85 cleanup, 2026-05-17, PR #135)

- **log-audit-20260517-002** (P2) — tickDrift -150000ms artifact after cadence transitions. Drift formula now reads currently-armed interval via `__atomic_load_n(&currentHealthIntervalMs, __ATOMIC_RELAXED)`; both re-arm sites zero `lastTickMs` so first post-transition sample reports `tickDrift=0`. Steady-state drift now matches pre-.84 baseline (~100ms). No functional impact (watchdog unaffected); telemetry hygiene only. **Acceptance MET 2026-05-18 audit**: 372/424 post-.85 samples = exactly 0ms, all others ±3ms; pre-.85 portion of same audit window had 721x -150000ms confirming the prior regression and the .85 fix exclusively addresses it.
- **codebase-audit-20260517-001** (P3) — `fastModeEntryMs` / `lastTickMs` int64 cross-task non-atomic. Both now `static volatile _millis_t` with `__atomic_store_n(RELEASE)` / `__atomic_load_n(ACQUIRE)` at all sites. Snapshot-once pattern at multi-check reads. Zero-byte change.

## Recently completed (.84 follow-up, 2026-05-17, PR #132)

- **log-audit-20260515-010 visibility gap** — .83's adaptive sampler missed the 3424B sub-180s dip because the trigger condition was only checked at slow-mode (180s) poll boundaries. .84 adds a 1Hz loopTask heap-watermark check via new `homekit_health_arm_fast_mode_if_low()` helper. Arms fast mode immediately on the drop, independent of poll cadence. Strictly additive — existing in-callback adaptive block continues to handle the stay-fast-5min-after-recovery logic. No FC / dedup / cooldown interactions.

## Recently completed (.83 bundle, 2026-05-16, PR #130 / commit 9dd52ea)

- **log-audit-20260515-010** (P1) — heap visibility: new ESP32-only `GET /heap` endpoint + adaptive HomeKit-health sampler cadence (180s → 30s when free heap < 20KB, holds 5min after recovery). 72h soak acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82, .83 NOT yet flashed)**: NEW deepest-dip-ever observed — heap=4000 maxBlock=2164 at 2026-05-15T19:58:26 CDT (uptime 167403s, ~46.5h). 4000B is 396B above the 4096B heap-floor short-circuit (yesterday's record was 4396, only 300B above). maxBlock=2164 means 54% of remaining heap fragmented into sub-2KB chunks — worst fragmentation snapshot ever recorded. Recovered to 42204 by next 180s sample (so dip lasted <3min). Single user activity in window: HK door open/close ~6 minutes prior (18:21:33 / 18:31:05 CDT) — heap had recovered to 42180 baseline by 19:55:26 then crashed 24KB at 19:58:26 with iOS quiet (last_hap_read_ago=899s, hintLevel=1). RSSI was -43dBm (worst observation in window, baseline -38dBm) suggesting lwIP rx-path or WiFi-stack consumption under marginal signal. SECOND DIP same window: heap=19320 maxBlock=21492 at 23:13:26 CDT, also 3-5min after door operations (Closing 23:03:06, Closing 23:07:50). Deploy of .83 with adaptive 30s cadence and `/heap` endpoint is the right next step — would have captured ~6 fast-samples bracketing the 4000-dip recovery curve. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed earlier today)**: NEW DEEPEST-DIP-EVER on .83 — heap=3424 maxBlock=1908 at 2026-05-16T11:16:18 CDT (uptime 12423s, ~3.4h post-OTA-boot). 3424B is 672B below prior 4000B record, only 672B above the 4096B heap-floor short-circuit. maxBlock=1908 = 56% sub-2KB fragmentation (worst ever, beats yesterday's 54%). last_hap_read_ago=33s (iOS active), RSSI=-37dBm baseline-normal — NOT correlated with marginal-signal hypothesis. No user activity within 30min prior. Recovered to 22064 (next 180s sample) then 39972 (sample after). **CRITICAL .83 FEATURE GAP CONFIRMED**: adaptive sampler 180s→30s acceleration line "HomeKit health sampler cadence: 180000ms -> 30000ms" was NEVER emitted for this dip — only the reverse 30000ms -> 180000ms line at 14:45:19 CDT (after the heap had been steady-state >40KB for 3.5h) appeared, meaning the gate WAS armed during the 11:16 dip but the dip occupied <180s and the next sample already showed 22064 (above the 20KB threshold), so cadence-acceleration ran for ≤1 cycle and the 180→30 announce never logged (or logged but the dip resolved before any 30s sample captured it). Net result: .83 adaptive sampler DOES NOT catch sub-3min transients — the very class of event we deployed it for. Need either (a) much higher poll rate (5-10s) when free heap < 30KB instead of < 20KB, or (b) interrupt-driven heap-low-watermark notification via FreeRTOS `heap_caps_register_failed_alloc_callback` style hook, or (c) sample-on-allocation-failure rather than time-poll. /heap endpoint not exercised in this window (would need on-demand fetch during a future dip — automate via the Pi).
- **log-audit-20260515-011** (P2) — SEC1 retry log enrichment: both first-of-burst and `[+N suppressed]` lines now include `[cmd=0xNN retries=N bus_silent=Nms cts=N rx_pending=N]` for root-cause attribution. 48h structured-log acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82)**: 2 more SEC1 TX retry events at 2026-05-15T23:02:52 and 23:07:37 CDT, both 1.0-1.5s before successful Open→Closing transitions (cmd transition timing identical to prior pattern). Pre-.83 log line still missing diagnostic suffix — confirms enrichment is needed; not a new pattern, just continued observation. Door closed cleanly each time (12047/12049ms). Cumulative SEC1-failure-then-success count now ~10 across .80/.81/.82 with zero functional impact. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed)**: 1 more SEC1 TX retry event at 2026-05-16T19:02:18 CDT (uptime 40386s, ~11.2h post-boot), enriched suffix `[cmd=0x39 retries=11 bus_silent=234ms cts=0 rx_pending=0]` — identical pattern to the 07:52:51 and 08:00:11 enriched events from earlier today's pre-audit window. Fired 1.5s before clean Open→Closing transition (12171ms close, no door reverse, no functional impact). Cumulative .83 enriched-SEC1 events now 3, all `cmd=0x39` (door state query) or `cmd=0x38`, all `retries=11 bus_silent~234ms cts=0 rx_pending=0` — pattern stable: GDO bus is silent for ~234ms during state-change moments, retries exhausted but actual command lands. Acceptance trending toward MET (need ~48h more enriched data).
- **log-audit-20260515-009** (P1) — HK Close-burst dedup + force-close reentry cooldown. New `hk_target_is_redundant()` helper at HK callback layer + `forceCloseClearedAtMs` stamp set in `clear_force_close_state`, gate in `door_command_force_close` (3s window). Strictly additive to existing FC safety gates (.80 release-callback fix, 2-attempt mechanic, CURR_CLOSED/CLOSING/0xFF entry gate, TAS). Code-review verified all 10 FC safety properties PASS.
- **log-audit-20260514-008** (P1) — HK Open-burst dedup: same `hk_target_is_redundant()` helper handles Open path. Mid-cycle reversals preserved.
- **tickDriftMs cast fix** (code-review out-of-scope finding from PR #130 review) — `src/homekit.cpp:688` time math fixed (was producing garbage int32-overflow values at ~25-day uptime).

## Deferred — soak-pending only

### [P3] HEAP-PRESSURE-WATCH — `mdns_networking: Cannot allocate memory` events under sustained operation
**Status:** deferred (`.79` heap-floor short-circuit makes the symptom non-lethal; OOM bursts no longer observed during overnight soak). Re-evaluate after .83 `/heap` + adaptive-cadence data identifies the transient allocator.
**Source:** 2026-05-09 .77 panic-loop investigation. Crash dump showed `free heap: 136 bytes` when mdns_mem_calloc failed. iOS hub state-sync + HomeSpan HAP + mDNS query bursts can momentarily consume nearly all heap. With `.79`'s fix (mdns task gated + 4KB heap floor) any such event is silently dropped at the syslog hook.
**Acceptance:** identify what's consuming heap during these bursts (heap-trace, periodic snapshots) and either bump tcpip MEMP pools / mdns rx-buf / cap HAP transaction concurrency, OR conclude the existing 30-50 KB headroom is enough now that the panic chain is fixed. Overnight `.79` soak shows heap stable at 38-42 KB — no OOM events in 10h+ — leaning toward "no further action needed" unless symptoms reappear.
**Notes:** panic chain is the priority — fixed at the syslog re-entry point so 100% heap exhaustion in mDNS no longer crashes. Investigate only if device experiences other symptoms (slow HAP, dropped notifications) under sustained low-heap conditions. Not user-visible right now.

---

## Upstream filing — DO NOT FILE

The fork's bug fixes (R1-R4 in `audit-notes/UPSTREAM_CHERRY_PICK_PLAN.md`) are already shipped in the fork. Their upstream-applicability is tracked in audit-notes/ for awareness only — **fork work proceeds regardless of upstream interest**.

**Hard rule:** No upstream PRs, no upstream issue filings, no cherry-picks to upstream. Issue #170 was filed historically; do not let it set precedent. Future agents reading this queue: if you find yourself drafting a `gh pr create --repo ratgdo/...`, STOP — that's not how this fork operates.

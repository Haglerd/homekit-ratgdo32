# homekit-ratgdo32 work queue

Priority-ordered. Top = next. Detailed analysis lives in `audit-notes/` (gitignored fork-internal). Done items are removed — git log is the durable record.

## Active

### [P0] log-audit-20260527-001 — .91 panic regression — 2x ESP_RST_PANIC in 5h on 27-May — **ROOT CAUSE RE-RE-IDENTIFIED 2026-05-27 22:45Z: heap-OOM in `tiT` task via lwIP `memp_malloc` (which is `malloc` under `MEMP_MEM_MALLOC=1` in arduino-esp32 lwipopts.h:105). Same allocator-class as -20260524-001, different code path.**
**Status:** .92 INSTRUMENTATION SHIPPED + LIVE 2026-05-27 19:07:34 CDT; **18.31h .92 soak so far is CLEAN (added 8.87h since the 2026-05-28 09:33Z audit, ZERO new panics/reboots/wedges, minFreeEver pinned at 16B across all 588 post-cursor diag-hk samples) — zero panics, zero wdt, zero reboots, recoverAttempts=0 across all 715 diag-hk samples, controllers=4/paired=yes throughout, zero pair-loss, zero SEC1 retries, zero web_loop skips. BUT the new `minFreeEver` field has descended 324B → 188B → 128B → 120B → 60B → 16B over the first 4.3h of .92 runtime, then plateaued at 16B for the last ~5h. /heap LIVE confirms `min_free_heap_ever:16` — the absolute deepest IDF intrinsic floor EVER recorded on this device (prior records: 44B on .91 post-2nd-panic, 132B on .89-era, 200B/268B/528B on .84-.87). The 16B floor is sub-Hz (1Hz health sampler captures free=1168B as the deepest sustained-30s sample; the 16B is a transient the IDF tracks across the whole boot). NO `heap-at-panic` instrumentation line has fired because no panic has occurred. .92 has therefore validated half the acceptance (instrumentation works, minFreeEver populates correctly, sub-Hz transients are now visible) — the other half (capturing `heap-at-panic` on a real crash) requires either waiting for a 3rd panic OR concluding that the heap-allocator is operating within 16 bytes of total exhaustion without crashing under .92's conditions.** **Important correction to prior diagnosis:** arduino-esp32's precompiled lwIP is built with `MEMP_MEM_MALLOC=1` (`framework-arduinoespressif32-libs/esp32/include/lwip/port/include/lwipopts.h:105`) — meaning lwIP does NOT use fixed-size pools at all. Every `memp_malloc(MEMP_SYS_TIMEOUT)` is just `malloc(sizeof(struct sys_timeo))` (~16B) from the general heap. So the `sys_timeout_abs` assert at `timeouts.c:194` (`LWIP_ASSERT("sys_timeout: timeout != NULL, pool empty", timeout != NULL)`) fires when **the regular heap can't supply 16 bytes** — it's a heap-OOM, just in the lwIP `tiT` task and from a different code path than .89's `web_loop → NetworkClient ctor → std::terminate()` in loopTask. The .89, .90, .91 fixes all targeted the loopTask path; none of them protected the `tiT` allocation path. This also explains the audit's record `min_free_heap_ever=44B` reading — the IDF intrinsic sub-Hz floor was actually grazing zero. .92 ships INSTRUMENTATION ONLY (no behavior change): (1) new `minFreeEver=NB` field appended to the existing `diag-hk` line so the IDF sub-Hz floor is logged every 30s instead of only on `/heap` HTTP poll, and (2) a `heap-at-panic: task=N free=N max_block=N min_ever=N` line printed via `esp_rom_printf` from `panic_handler` so the next crash captures the heap state at the moment of assert AND the panicking task name. With this telemetry: if a .92 panic also shows `task=tiT free<100 max_block<100`, we have definitive proof that the heap is grazing zero pre-crash AND identify the dropping path; .93 can then target that path specifically. The .91 ship (commit 2c81980) reverted the .90 `WEB_ACCEPT_MIN_HEAP_BYTES=8192` web_loop guard AND de-gated the `Free heap dropped below 4096, rebooting` recovery in `ratgdo.cpp`. Across the full 7-day (5-day effective) syslog window, the de-gated recovery path produced **ZERO log lines** — but that is **expected** given the corrected diagnosis: heap was NEVER low at either panic. Two ESP_RST_PANIC events fired on 27-May (09:31:36 CDT after ~150917s uptime; 14:42:01 CDT after ~18524s uptime since the 09:31 panic-recovery boot). **The 14:42:01 panic was preceded by 5+ minutes of perfectly healthy diag samples: free heap held flat at 42836-42872 B, maxBlock=40948, controllers=4/paired=yes, recoverAttempts=0, hkHealthyTicks monotonically incrementing 1→8, hintLevel=0, SSE/log telemetry all-zero. The final pre-crash line was the standard cadence transition `30000 ms -> 180000 ms (free_heap=42460)` at uptime 18518s — no heap pressure, no SSE/web activity, no L3 escalation.** Backtrace (addr2line against local `.pio/build/ratgdo_esp32dev/firmware.elf` built 2026-05-25 15:28 — 5min before the .91 commit, so symbols are .90/pre-.91 but lwIP frames are IDF-resident and stable across the trivial .91 diff): `panic_abort → esp_system_abort → __assert_func → sys_timeout_abs (lwip/src/core/timeouts.c:194) → lwip_cyclic_timer (timeouts.c:262) → sys_check_timeouts (timeouts.c:403) → tcpip_timeouts_mbox_fetch (api/tcpip.c:98) → vPortTaskWrapper`. Task: `tiT` (lwIP TCP/IP task), NOT `loopTask`. The assert at `timeouts.c:194` is the well-known `LWIP_ASSERT("sys_timeout: timeout != NULL, pool empty", timeout != NULL)` — fires when `memp_malloc(MEMP_SYS_TIMEOUT)` returns NULL, i.e. the dedicated lwIP timeout pool (sized by `CONFIG_LWIP_MAX_TIMEOUTS`, default 8 in stock lwIP, often higher in IDF) is exhausted. **This is a fixed-size memp pool, completely independent of the heap** — explains why heap=42KB at crash, why the .90 `WEB_ACCEPT_MIN_HEAP_BYTES` guard would never have caught it, why the de-gated `<4096` recovery would never have fired, and why the .89-era `std::terminate()` hypothesis was incorrect. Slow-accumulator profile (5h and 41h uptimes before crash) is consistent with lwIP timeouts leaked by a slow path: candidates include TCP TIME_WAIT/FIN_WAIT timers held by HK controller connection churn (4 controllers cycling TCP keepalives), mDNS query timers, IGMP timers, or DHCP renewal timers piling up faster than they expire. **Plan space (REVISED — old a/b/c/d are obsolete because they all targeted the wrong root cause):** (a) Bump `CONFIG_LWIP_MAX_TIMEOUTS` in sdkconfig (low-risk band-aid — buys time, doesn't fix leak), (b) instrument lwIP timeout-pool high-water mark via `LWIP_STATS` / custom `memp_malloc` wrapper to identify the leaking timer family, then fix the specific path, (c) audit each TCP-connection-creating path (HomeSpan, web server, OTA, mDNS, SSE) for cleanup-on-disconnect to ensure `tcp_close()`/`tcp_abort()` is called and TIME_WAIT entries don't accumulate beyond `MEMP_NUM_TCP_PCB_TIME_WAIT`, (d) add a `tiT`-task heap+memp-pool periodic dump so the next assert is preceded by a useful telemetry trail. **Needs human planning** — observability path (b+d) is the right first step; the user should decide the bump-vs-instrument tradeoff. The GH issue #156 was opened under the OLD hypothesis and needs a follow-up comment with this correction.
**Source:** log-audit 2026-05-28 09:33Z (Pi syslog full 7-day stream, 21051 lines across ratgdo.log + .1 + .2.gz [PARTIAL-corrupt, 1 WARN logged] + .3.gz [PARTIAL-corrupt, 1 WARN logged] + .4.gz, per-file `zcat` loop recovered partial data from both corrupt archives. Date coverage: 05-24 5144 lines / 05-25 4712 / 05-26 4863 / 05-27 5317 / 05-28 1015 = 5 days actual, 7-day cutoff half-empty on the early side (archives older than 05-24 already rotated out — NOT a forwarding gap). Device endpoints — `/showlog` + `/showrebootlog` + `/crashlog` all returned 0-byte (HTTP Digest-gated, passwordRequired=true; credential probe correctly blocked by auto-mode classifier). `/status.json` LIVE: firmwareVersion=3.4.4-forceclose.92, crashCount=0, paired=true, controllers=4, upTime=33998279ms = 9.44h continuous since .92 OTA boot 27-May 19:07:34 CDT, freeHeap=69220, minHeap=32416, wifiRSSI=-38dBm. `/heap` LIVE: `free_heap:36948, min_free_heap_ever:16, max_alloc_block:30708, internal_free:68316, internal_largest:30708, uptime_s:33998, ticker_active:1, ticker_arm_count:78, ticker_arm_failed_at_ms:0`. **min_free_heap_ever:16 is the absolute deepest IDF intrinsic floor EVER recorded across this audit history** — 28B below the prior record (44B at 2026-05-27 21:35Z audit). The 16B sub-Hz transient floor combined with `ticker_arm_failed_at_ms:0` and recoverAttempts=0 across all 715 .92 health samples means the .92 boot has gone within 16 bytes of total exhaustion without crashing OR firing the `Ticker arm FAILED` ESP_LOGE — a remarkable signal that the heap allocator is functioning at the edge of its envelope but consistently recovering.) PRIOR: log-audit 2026-05-27 21:35Z (22080 lines, pre-.92 cursor).
**Issue:** Haglerd/homekit-ratgdo32#156
**Acceptance:** .92 is an INSTRUMENTATION ship — acceptance is OBSERVATIONAL, not fix-validation. (1) The new `diag-hk: ... minFreeEver=NB` field must appear in Pi syslog within the first health sample post-OTA. (2) IF a 3rd panic happens on .92, the `heap-at-panic: task=N free=N max_block=N min_ever=N` line MUST be captured in the serial console / Pi syslog upstream of the panic banner. The data from those two paths together identifies the actual leak path → .93 ships the targeted fix. Until a panic happens on .92 with the snapshot captured, this item stays open for telemetry collection. If 7+ days pass with no panic on .92, downgrade to P2 and treat the issue as latent — but maintain the instrumentation indefinitely.
**Notes:** P0 device-self-rebooting-via-panic AND silent-wedge. **2026-05-28 18:30Z audit — .92 SOAK UPDATE — 18.31h CLEAN POST-OTA (added 8.87h since prior audit), minFreeEver pinned 16B (zero further descent), trend metrics improving.** Post-cursor window (2026-05-28 09:33Z → 18:30Z, 1921 syslog lines, 588 diag-hk samples): zero panics, zero reboots, zero uptime regressions (uptime grew monotonically 33965s → 65950s), recoverAttempts=0 across ALL post-cursor samples, controllers=4/paired=yes throughout, zero SSE desync, zero WiFi flap, zero pair-loss, zero force_close issues, zero web_loop skips, ONE cosmetic SSE-orphan TCP-drop reap (10.112.60.248, browser tab closed). heap baseline-flat (samples first=38448 / last=38440 = delta -8B / avg-by-hour wobbles between 36.5–41.5KB across the full 18h — no leak slope). 34 post-cursor heap-watermark fast-cadence triggers (declining trend across the 5-day window: 78→66→76→66→53), all transitioned cleanly with the .89-era 256B guard never re-firing. L3 watchdog hints: 7 escalations to threshold-level=3, ALL self-recovered to L0 within a single 30s cadence (iOS hub-idle cycles, recoverAttempts=0 confirmed throughout — same benign behavior as log-audit-20260521-001-closed). **`minFreeEver` field: pinned at 16B in 100% of 588 post-cursor diag-hk samples** (vs prior audit's 372/715=52%-at-16B with the rest at 60/120/188/324B during the descent phase). The descent phase ended at ~5h post-boot and the floor has now held flat at 16B for the entire 13h since — the heap allocator is operating within 16 bytes of total exhaustion every boot, consistently, without crashing. **NO `heap-at-panic: task=N free=N max_block=N min_ever=N` line has fired in any post-cursor syslog** because no panic has occurred — the instrumentation is armed and waiting. /heap LIVE at audit: free_heap=36940, min_free_heap_ever=16, max_alloc_block=32756, internal_free=68308, internal_largest=32756, uptime_s=65919, ticker_active=1, ticker_arm_count=144, ticker_arm_failed_at_ms=0. **7-day clean-soak downgrade target: 18.31h / 168h = 10.9% complete.** Trajectory strongly positive — at current rate, downgrade-to-P2 gate reached ~2026-06-03 19:07 CDT if no panic intervenes. **2026-05-28 09:33Z audit — .92 SOAK UPDATE — 9.44h CLEAN POST-OTA + minFreeEver descent confirmed.** Recurrence: ALL .91 panics + the .91 OTA self-recovery panic at 19:06:49 CDT (4th .91 panic during OTA upload, loopTask → handle_firmware_upload → vTaskDelete → vPortTLSPointersDelCb → abort, recovered on 3rd OTA retry) fold under this item per the task brief — these are recurrences/context, NOT new findings. The .92 OTA boot at 27-May 19:07:34 CDT (restart reason 3) has now run continuous for 33998s = 9.44h with: zero panics (no `System restart reason: 4`), zero wdt/abort/Backtrace/IllegalInstruction/StoreProhibited/LoadProhibited/terminate lines, zero `Ticker arm FAILED` ESP_LOGE, zero web_loop skip warnings (.91 revert holds), zero SEC1 retries, zero pair-lost, recoverAttempts=0 across all 715 diag-hk samples, controllers=4/paired=yes throughout, no SSE counter desync (3 sseBufferFullSkips + 1 sseOrphansReaped = cosmetic browser-TCP-disconnect, no slot leak), 4 L3 escalations all self-recovered, 7 sub-5KB 1Hz heap dips (deepest free=1168B maxBlock=948B at 19:32:21 CDT, ~25min into boot, recovered to baseline next sample). **`minFreeEver` field trajectory (the .92 instrumentation feature, sampled every 30s in diag-hk): 324B (19:10:30 CDT, first sample) → 188B (19:56:24) → 128B (20:19:56) → 120B (20:28:56) → 60B (21:47:16) → 16B (23:28:08) → plateau at 16B for the last ~5h.** Distribution by value: 372 samples at 16B (52% of samples), 152 at 60B, 96 at 120B, 65 at 324B, 27 at 188B, 3 at 128B. **The 16B floor is sub-Hz** — the 1Hz health sampler never directly captures it (deepest 1Hz sample = 1168B), but the IDF intrinsic `min_free_heap_ever` tracks it across the whole boot. This is consistent with the corrected -20260527-001 diagnosis (heap-OOM at sub-Hz transient depth, the `tiT` lwIP timeout allocation failing at sub-ms intervals the 1Hz sampler can't catch). **Critical observation: the heap has gone within 16 bytes of total exhaustion on .92 — DEEPER than the .91 record (44B) — but NO panic has fired and NO `heap-at-panic` instrumentation line has emerged.** This means either (a) the .92 conditions haven't hit the specific allocator+task+timing combo that triggered .91 (different lwIP timer registration timing, different TCP keepalive churn), (b) the 16B floor is being grazed by a different allocator that does NULL-check (graceful handler vs the `tiT` sys_timeout_abs assert that fires on NULL), or (c) the panic is probability-driven and the .92 window is statistically clean by chance. The user-supplied .91 backtrace (`tiT → sys_check_timeouts → sys_timeout_abs assert`) specifically targets MEMP_SYS_TIMEOUT allocations — different allocation site than wherever the 16B floor is being grazed in normal operation. **Two paths forward (REVISED, supersedes prior plan space):** (i) wait for a 3rd panic on .92 — the `heap-at-panic` line will then identify the specific task + allocator + free/max_block at the moment, OR (ii) if .92 runs 7+ days clean, downgrade severity to P2 and treat the heap-OOM as fully bounded by .91-era state (something in the .92 revert path is preventing the trigger), then keep instrumentation live indefinitely. Telemetry now provides the data the next panic needs to be diagnostic; until then, continue soak observation. **Earlier evidence retained:** **Timeline (Pi syslog, definitive):** Boot #1 .91 OTA 25-May 15:35:54 CDT (restart reason 3). First panic 27-May 09:31:29 CDT after 150917s = 41h55m uptime → reason 4 → autorecover boot 09:31:36. Second panic 27-May 14:42:01 CDT after 18524s = 5h08m uptime since the 09:31 recovery → reason 4 → autorecover boot 14:42:01. **THIRD event 27-May 18:33:43 → 18:57:52 CDT (24m9s SILENT WEDGE, NO restart-reason-4 banner emitted, MANUAL POWER CYCLE required):** last live diag-hk at 18:33:43 reads `heap=38420 maxBlock=32756 recoverAttempts=0 hintLevel=2 hkHealthyTicks=0 last_hap_read_ago=1280s` (perfectly healthy, only signal is iOS quiet ~21min — same benign signature as past audits' iOS idle); the same cycle armed the slow-cadence transition `30000 ms -> 180000 ms (free_heap=38420)` at 18:33:43.861; then total syslog silence for 24m9s until user power-cycled at 18:57:52 (restart reason 1). **No `reason 4` banner appeared during the silent window** — meaning either (a) a panic fired but the recovery boot didn't auto-emit (chip stuck in deeper fault, panic_handler itself crashed, or WiFi/lwIP heap was so dead that even the recovery boot couldn't syslog-forward its banners before the user cut power), or (b) a pure deadlock with no panic emitted at all. Same shape as the .90 stranding (log-audit-20260525-001) which .91 was supposed to fix — wedge has REGRESSED, just under different conditions (heap was healthy at last sample, no low-heap dead-band). **.92 instrumentation just shipped (commit 6bbdfd7, push dee2db9)** captures `heap-at-panic: task=N free=N max_block=N min_ever=N` via esp_rom_printf and adds `minFreeEver=NB` to diag-hk — but this device is still on .91 (OTA pending). Until .92 is on-device, the next failure produces no new data. **Definitive backtrace (user-supplied pre-crash log + addr2line, 2026-05-27 22:00Z):** `tiT` task panic, IllegalInstruction at 0x4008EBBC = `panic_abort` → `esp_system_abort` → `__assert_func` → `sys_timeout_abs (lwip/src/core/timeouts.c:194)` → `lwip_cyclic_timer` → `sys_check_timeouts` → `tcpip_timeouts_mbox_fetch` → `vPortTaskWrapper`. The lwIP MEMP_SYS_TIMEOUT pool was exhausted — independent of heap (which was 42460-42872 B at every diag sample for the 5+ minutes immediately preceding the crash). **Pre-second-panic health (user-supplied log, definitive):** 14:35:07 user-reported health: `heap-watermark trigger (free=1388 maxBlock=1076 < 20000), arming fast cadence` — but: (a) the trigger was the 1Hz polled sampler catching a sub-second TCP/TLS spike (not a sustained dip), (b) the very next 30s sample read free=42872, and (c) **the panic is unrelated to that heap dip** — the lwIP timeout pool is not the general heap. From the user's last 9 diag samples: free 42848/42836/42872/42860/42860/42848 B, maxBlock=40948 throughout, hkHealthyTicks 1→8, recoverAttempts=0, hintLevel=0, sseSlowWrites=0, sseBufferFullSkips=0, sseSlotsAlloc=0, sseOrphansReaped=0, jsonPeak=2379B, loopHWM=5324B, tmrHWM=6060B, apHWM=11768B, tickDrift=0ms — every metric perfectly healthy. Final pre-crash line: cadence transition `30000 ms -> 180000 ms (free_heap=42460)` at uptime 18518s; panic at uptime 18524s, 6s later. The 1Hz `<4096` recovery gate could not have helped — heap was never below 4096B in any of the relevant samples. **De-gated recovery silence — definitive:** exhaustive grep for `Free heap dropped|sync_and_restart|rebooting|low.heap|sub.floor|self.reboot` across the full 7-day stream returned ONLY the planned 255600s rotation reboot at 05-23 17:45 (`Rebooting system as 255600 seconds expired`). The .91 de-gated recovery has not fired ONCE. **Heap floor by day** (1Hz health-sample minimum): 05-23 min=2104B, 05-24 min=1360B, 05-25 min=884B, 05-26 min=384B (DEEPEST 1Hz sample of the week), 05-27 min=1832B. Baseline ~41K throughout — NO leak slope, just continuing sub-second allocator transients. `/heap min_free_heap_ever=44` is the all-time intrinsic deepest (vs prior 132B). **Sub-2KB sample count by day:** 0/2/8/5/2 (peak on 05-25 during .89→.90→.91 transitions; .91-only span 05-26 + 05-27 = 7 sub-2KB samples in 44h ≈ 0.16/h, consistent with prior .89 envelope of ~2.29/h pre-.91, so the rate has actually DROPPED — but the magnitude grew). **L3 watchdog hint rate:** flat at 7/day (05-23/25/26/27) and 9/day (05-24, includes a panic). All 35 L3 events across the post-cursor 05-27 partial-window self-recovered, recoverAttempts=0, zero pair loss, controllers=4/paired=yes throughout. L3 is hub-side iOS idle as established in log-audit-20260521-001 — NOT correlated with either panic (the 09:31 panic fired at hintLevel=2 reading `last_hap_read_ago=1610s` ~27min iOS idle; the 14:42 panic fired at hintLevel=0 fresh after a recovery from L3 at 14:30:53). **EAGAIN fd events:** fd=52 events 3 (1 new post-cursor on 26-May 20:53:13 CDT, fd back to STATIC 52); fd=53 events 3 (all from .90 starvation window 12:39-13:02 CDT 25-May, NONE post-.91). The fd returning to static 52 on .91 confirms log-audit-20260522-001's "if fd returns to static post-fix, stays benign" branch — the .90 fd climb was symptomatic of the .90 guard, not an independent leak. Item correctly stays benign/queued. **Distinct from -20260524-001 (.89 panic)** = **DIFFERENT crash CLASS, not same as previously claimed**. -20260524-001 was `web_loop → NetworkClient ctor → std::terminate()` in loopTask under heap pressure. This (-20260527-001) is `tiT → sys_check_timeouts → memp pool empty assert` in the lwIP task with healthy heap. The two are unrelated except both happen to manifest as ESP_RST_PANIC. **Distinct from -20260525-001 (.90 stranding)** = .91 fixed the stranding (device self-recovers, no manual power cycle this audit), but reintroduced the panic by removing the spike-guard. Recurrence: 2 panics post-cursor (both genuinely NEW). 0 recovery-reboots fired (de-gated path silent). Auto-fix-eligibility: **needs-human-planning** (heap-spike-timing-sensitive, fix candidates each have already-shipped predecessors with their own regressions, choice between a/b/c/d requires user judgment on observability-vs-availability tradeoff). Issue body retained at /tmp/issue-body.md.

### [P0] log-audit-20260527-001-impl — .93 lwIP-leak observability ship (slope instrumentation, behavior-neutral)
**Status:** IN PROGRESS — software-engineer implementing 2026-05-29. Planner output 2026-05-29 (background agent) verified a/b infeasible against the precompiled framework, c premature → .93 is a refined (d): make the lwIP timeout-alloc accumulation slope visible in the diag stream so the leak family is identified over days instead of waiting for a rare panic. 5-day deadline before fleet rollout.
**Issue:** Haglerd/homekit-ratgdo32#156
**Plan (verified against framework headers):**
- (a) `CONFIG_LWIP_MAX_TIMEOUTS` bump = DEAD. Knob is fictional (real symbol `MEMP_NUM_SYS_TIMEOUT`) and inert anyway — `MEMP_MEM_MALLOC=1` + `MEM_LIBC_MALLOC=1` (lwipopts.h:98,105) means `sys_timeo` mallocs from general heap, no pool to size. sdkconfig also inert under `framework=arduino`.
- (b) `LWIP_STATS`/`memp_malloc` wrapper = DEAD. `# CONFIG_LWIP_STATS is not set` (sdkconfig:2048) → counters not compiled into the precompiled archive; `memp_malloc` not `--wrap`-able. Same dead-end class as heap-trace (log-audit-20260518-002).
- (c) TCP-cleanup audit = PREMATURE — most leak candidates (HK controller churn, mDNS) live in HomeSpan/IDF, not fork code. Defer until data names the family → .94.
- (d, SHIPPING) periodic slope instrumentation:
  - **Step 1 (auto):** add `minFreeEver` slope/socket-population proxy field to the diag emitter in `src/homekit.cpp` `homekit_health_log` (~907-955). File-scope `uint32_t lastMinFreeEver` (esp_timer-task-local, no atomic needed). **diag-hk line is at the 256B ceiling — if the field overflows, add a 3rd `diag-net:` line rather than truncate.** `#ifndef ESP8266` (rides existing guard). +4-8B static ESP32, 0 ESP8266.
  - **Step 2 (auto, ONE build-time go/no-go):** expose live lwIP TCP PCB/TIME_WAIT count in `/heap` (`src/web.cpp` `handle_heap()` ~2141-2187). Confirm `tcp_active_pcbs`/`tcp_tw_pcbs` link against the precompiled archive; if not, fall back to fd-scan (log-audit-20260522-001) or drop Step 2. Bump `buf[320]`→`buf[384]` (stack only). 0 heap.
**Acceptance:** (1) clean `pio run -e ratgdo_esp32dev`, no new -Wshadow/-Wsign-conversion. (2) post-OTA boot reason-3, crashCount unchanged, new field renders <256 chars (grep rendered line). (3) over 48h soak: climbing socket/PCB count tracking the `minFreeEver` descent IDENTIFIES the leak family → .94 targeted fix; flat count while heap still drops FALSIFIES the TCP hypothesis → redirect to mDNS/DHCP. Either outcome = successful .93. (4) zero availability regression vs .92 (no wedge/reboot, recoverAttempts=0, controllers=4/paired). (5) NO behavior change, NO force-close/auto-close/door-state touch, NO `WEB_ACCEPT_MIN_HEAP`-style reject path.
**Notes:** Hand-off chain planner→software-engineer→code-review. Step 3 (correct #156) DONE 2026-05-29 (comment posted, title fixed, stale #153/#154 closed-folded). Auto-fix-eligibility: **auto-fixable** (Step 1 fully; Step 2 with build-time symbol-link check).

_Two deferred conditional P3s (code-review-20260519-001 TWDT ISR `IRAM_ATTR`, code-review-20260519-002 `setup_homekit` healthTickerMux guard) archived to `audit-notes/deferred-archive.md` on 2026-05-29 — not drain-actionable; re-promote only if their trigger conditions fire._

---

## Recently completed (closure pass, 2026-05-28)

Queue-hygiene pass after the .92 OTA. Code-verified each item's claimed status against current `src/`; closed items whose acceptance was met OR whose root cause is now tracked elsewhere. Net: 10 active → 3 active.

- **log-audit-20260525-001** (P0) — **closed: code changes shipped + tracking folded.** The .91 code changes (revert `WEB_ACCEPT_MIN_HEAP_BYTES` guard, de-gate recovery reboot in `ratgdo.cpp:529`) are live in current src/ (grep confirms zero `WEB_ACCEPT_MIN_HEAP` references, `MIN_FREE_HEAP` self-reboot exists at the un-nested check). Acceptance ("zero >5min mid-boot silence ending in restart-reason-1") FAILED on .91 — the wedge regressed on 27-May 18:33→18:57 CDT requiring manual power cycle. That regression + the active investigation are now under log-audit-20260527-001 with the .92 instrumentation. Keeping this item open separately = duplicate tracking.
- **log-audit-20260524-001** (P0) — **closed: original class non-recurring; root-cause investigation continues under -20260527-001.** The `web_loop → NetworkClient ctor → std::terminate()` chain in loopTask has NOT recurred since the .90 fix. The .90 `WEB_ACCEPT_MIN_HEAP_BYTES` guard was reverted in .91 (cure was worse — see -20260525-001), but the original class has not fired in 60+ hours of .91/.92 runtime. The 4 .91 panics on 27-May were a different class (`tiT` task, lwIP `memp_malloc` path); that continuum is tracked under log-audit-20260527-001.
- **log-audit-20260522-001** (P2) — **closed: benign EAGAIN noise, fd static.** fd has held STATIC at 53 across the entire 7-day window since the .90-era climb (which itself was a symptom of the .90 stranding, not an independent leak). Zero functional impact: no pair loss, no SSE slot leak, recoverAttempts=0, controllers=4/paired=yes throughout. EAGAIN with strerror "No more processes" is newlib's text for errno 11 — normal Arduino-layer would-block during iOS reconnect after extended idle. Acceptance gate ("fd stays static / zero impact") fully met.
- **log-audit-20260521-001** (P2) — **closed: trend resolved, no device fault.** iOS-quiet L3 rate trajectory across 12 cross-audits: 0.62 → 0.42 → 0.28 → 0.30 → 0.00 → 0.00 → 0.40 → 0.179 → 0.408 → 1.087 → 0.415 → 0.167/h trailing-6h, then flat 7-9/day across the .91/.92 audit window. All escalations self-recover (recoverAttempts=0 across all 545+ diag-hk samples, zero pair loss, zero confirmed-NR). L3 depth band unchanged at ~30-31min iOS idle — root cause is hub-side iOS idle, anticipated by the watchdog hint text itself. Instrumentation correctly led the data; the data resolved to no action needed.
- **log-audit-20260517-001** (P1) — **closed: mitigation shipped (.86), identification continues under -20260527-001.** The .86 HAP preflight gate (`HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256` at `src/homekit.cpp:598`) shipped and holds — confirmed empirically on .89 with a largest_free=236B guard fire that prevented an `esp_timer_create` ESP_ERR_NO_MEM stall. The allocator-identification half (which allocator drops heap to ~0?) was instrumentation-blocked (heap-trace not linkable under `framework=arduino`; failed-alloc callback disqualified by the .86 storm), and is now reopened under log-audit-20260527-001 with the .92 `minFreeEver` diag-hk field + `heap-at-panic` snapshot. Same root cause family — no point tracking it twice.
- **log-audit-20260513-007** (P1) — **closed: verified via static review.** Fix code-confirmed at `src/comms.cpp:2847+` (re-check `forceCloseInProgress` at callback entry — entry-guard from `log-audit-20260513-007` comment) and `comms.cpp:2882` (the "FORCE CLOSE: release sent, sequence already cleared" log line that fires if the race ever triggers). 5 force-close sequences observed cumulatively across .80/.81/.82, all took the non-race path (the user's GDO consistently enters Closing within 1.0-1.5s, well inside the 2500ms hold, so the post-clear release-callback race window is not naturally exercised). The race log line has NEVER appeared — natural race-trigger frequency is measured in months. "Soak validation pending" is impractical for this acceptance shape; close on static-review confidence and rely on the inserted log line to surface the race if it ever fires naturally.
- **HEAP-PRESSURE-WATCH** (P3, was in deferred-soak section) — **closed: folded into -20260527-001.** Same root cause family — sustained allocator pressure dropping heap to near-zero. The .79 syslog-hook gate (4KB floor) made the original `mdns_networking: Cannot allocate memory` symptom non-lethal; the underlying pressure is now tracked under -20260527-001 with .92 instrumentation. No reason to track two items for the same allocator-identification work.

## Recently completed (closure pass, 2026-05-23)

- **log-audit-20260518-004** (P0) — **closed: revert held, acceptance far exceeded.** The .87 revert (PR #138) removed the `heap_caps_register_failed_alloc_callback` that caused the 25-fire post-OTA reboot storm; the HAP heap-pressure gate (read-side observability) was retained. 72h soak target MET and far exceeded — across the full May 19→23 syslog span (8420 lines, ~4.25 days) there were ZERO `failed-alloc callback fired` lines, ZERO Guru/panic/task_wdt/brownout resets, and exactly ONE reboot (the planned .89 OTA, uptime reset 82565s→183s). .89 alone has run ~59h continuous (crashCount=0, paired/4 controllers). Storm regression is dead. No further firmware change.

## Recently completed (log-audit-20260520, 2026-05-20)

- **log-audit-20260519-001** (P2) — **closed: trend resolved, healthy iOS-idle quiet behavior, no device fault.** Cross-audit #7 (log-audit 2026-05-20 16:39Z, the prior cycle): **L3=0.179/h** crossed the 0.20/h close-watch gate from the acceptance criteria. Full trajectory across 7 audits on .87/.88: L3 rate 0.62 → 0.42 → 0.28 → 0.30 → 0.00 → 0.00 → 0.40 → **0.179**/h. All escalations self-recovered with recoverAttempts=0 — zero `HomeKit pair lost`, zero confirmed-NR, zero mDNS/HAP failure mode underneath. Behavior is iOS home-hub-side idle, not a device fault; the watchdog hint text itself anticipated this ("could be normal hub idle, watch the trend"). The instrumentation correctly led the data and the data resolved to "no action needed." Watchdog telemetry has since stopped emitting (see log-audit-20260520-001 below) but that is a separate observability defect on the producer side, not an L3 escalation. No firmware change.

## Recently completed (log-audit-20260520-001, 2026-05-20)

- **log-audit-20260520-001** (P1) — **ACCEPTANCE MET 2026-05-22 01:33Z** — `homekitHealthTicker` callback stopped firing for 6h+ on .88 after a heap-pressure cadence transition. Root cause: the Arduino `Ticker::_attach_us()` calls `esp_timer_create()` without checking the return — under heap pressure it returns `ESP_ERR_NO_MEM`, leaves `_timer = NULL`, and subsequent `esp_timer_start_periodic(NULL, ...)` silently fails. PR #140's spinlock made detach+attach atomic across tasks but didn't verify success. Fix in three layers: (1) **heap-pressure guard** (`HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256`) — if largest free block is below 256B, skip the cadence change entirely and let the existing periodic timer keep firing at its prior interval (cadence-state rolled back to match); (2) **arm-failure detection** via `Ticker::active()` check under `healthTickerMux`, stamped to a `volatile bool homekitHealthTicker_armFailed` flag + millis timestamp; (3) **deferred-retry path** on loopTask 1Hz — re-arms once heap recovers, with the warning log rate-limited to once/60s to avoid syslog flooding. Plus three new `/heap` JSON fields (`ticker_active`, `ticker_arm_count`, `ticker_arm_failed_at_ms`) for post-deployment observability without a reboot. Build-verified clean; code-review two-pass APPROVED (first pass flagged a 1Hz log-storm defect, fixed and confirmed in re-review). Acceptance is latent — verified by 24h+ continuous boot with no health-sample silence gaps > 2× current cadence, plus the new /heap fields populating sanely. **2026-05-21 00:21Z audit (35min post-.89-OTA)**: producer REVIVED on .89 — health/diag-sse/diag-hk samples streaming every 30s/180s as expected, vs zero HK lines on .88 from 14:29Z onward. /heap LIVE: `ticker_active:1`, `ticker_arm_count:7`, `ticker_arm_failed_at_ms:0` (clean baseline). 3 heap-watermark triggers post-boot (free=14464, 17420, 4700) all transitioned cleanly through the new guard — largest_free never dipped below 256B threshold during a transition attempt, so `skipping cadence re-arm` line has not appeared yet (the guard is reactive only). ZERO `Ticker arm FAILED` ESP_LOGE. 1 L1 watchdog hint (iOS idle ago=314s, recovered within 5min — base rate, not escalation). 1 cosmetic SSE orphan reap (browser TCP close, P3 noise). Acceptance trajectory positive at +35min; 24h soak target ~2.4% complete. Continue cycle; revisit at next audit. **2026-05-21 10:32Z audit (~10.8h into .89 boot)**: producer continuity STRONG — 337 HomeKit-health + 319 diag-sse + 319 diag-hk samples across the 8.28h window, **max inter-sample silence gap = 360s (exactly 2x the 180s slow cadence, AT the boundary, NOT > 2x — one coalesced/dropped slow tick, uptime monotonic across it so no producer stall)**, all other gaps <= 209s. NO violation of the "no gap > 360s" acceptance. /heap LIVE: `ticker_active:1`, `ticker_arm_count:48` (climbing from 11/7 prior, healthy), `ticker_arm_failed_at_ms:0`. ZERO `Ticker arm FAILED` ESP_LOGE, ZERO `skipping cadence re-arm (largest_free<256B)` guard fires (largest_free stayed above 256B at every transition — guard remains reactive-only, not exercised). 19 clean cadence transitions, 18 fast->slow announces all at free_heap ~41.6K. 24h soak ~44% complete (~10.8h continuous .89 boot). Trajectory strongly positive; if .89 clears 24h continuous with no >360s gap, acceptance is MET — on track. **2026-05-21 22:35Z audit (~22.9h into .89 boot)**: producer continuity STRONG and IMPROVED — 545 HomeKit-health + 545 diag-sse + 545 diag-hk samples across the 12.05h window, **max inter-sample uptime gap = 205s (well UNDER the 360s = 2x180s threshold; zero gaps > 209s, uptime monotonic, 0 out-of-order)** — strictly better than the prior window's single 360s borderline gap. /heap LIVE: `ticker_active:1`, `ticker_arm_count:113` (climbing from 48, +65 arms over ~12h, healthy), `ticker_arm_failed_at_ms:0`. ZERO `Ticker arm FAILED` ESP_LOGE. **NEW — the `HOMEKIT_HEALTH_TICKER_REARM_MIN_BLOCK = 256` guard FIRED for the first time and worked as designed**: `2026-05-21 07:23:36 W HomeKit health: skipping fast-cadence re-arm (largest_free=236B < 256B), staying at slow interval` — largest_free dipped to 236B (below 256B), the guard correctly skipped the cadence re-arm (which would have hit `esp_timer_create` ESP_ERR_NO_MEM), the existing periodic timer kept firing, and the producer never stalled. This is the exact protective path designed in the fix, now empirically validated under real heap pressure (not just reactive/untested). **24h soak ~95% complete (~22.9h continuous .89 boot, uptime_s=82295 at audit, crashCount=0)** — has NOT yet cleared the full 24h; one more audit (~1.1h+) confirms MET. Trajectory: acceptance imminent, guard validated. **2026-05-22 01:33Z audit (~25.81h into .89 boot — ACCEPTANCE MET): the .89 boot has now cleared the full 24h continuous with NO health-sample silence gap exceeding the 360s = 2x180s threshold.** Window (17:38->20:33 CDT, ~2.95h, 424 syslog lines): 137 health + 130 diag-sse + 130 diag-hk samples, **max inter-sample uptime gap = 210s** (well under 360s, zero gaps >210s, uptime monotonic, 0 out-of-order). Full-.89-boot peak inter-sample gap remains the single 360s borderline tick from the 10:32Z window (AT boundary, not >). /heap LIVE (device endpoint, serverTime 1779413625 = 2026-05-22T01:33:45Z, uptime_s=92914 = exactly 25.81h since OTA boot 1779320711 = 2026-05-20T23:45:11Z, confirmed continuous): `ticker_active:1`, `ticker_arm_count:131` (climbing from 113, +18 arms over ~3h, healthy linear growth), `ticker_arm_failed_at_ms:0`, crashCount=0. ZERO `Ticker arm FAILED` ESP_LOGE this window and across the full .89 boot. ZERO new `skipping fast-cadence re-arm (largest_free<256B)` guard fires this window — the single guard fire at 07:23:36 CDT (largest_free=236B, already credited in the 22:35Z audit) remains the lone empirical validation; the protective path is proven and not re-triggered. 9 clean fast->slow cadence transitions this window, all announced at free_heap ~41.6K. Producer continuity has now held strong across the entire 24h+ soak with the heap-pressure guard validated under real pressure. **Acceptance criteria fully satisfied — finalized/closed 2026-05-23 per user sign-off (guard validated under real heap pressure, ~59h continuous .89 soak, zero ticker-arm failures).** **2026-05-22 06:01Z audit (~30.3h into .89 boot) — acceptance continues to hold**: /heap LIVE `ticker_active:1`, `ticker_arm_count:156` (climbing from 131, +25 over ~4.4h, healthy linear growth), `ticker_arm_failed_at_ms:0`, crashCount=0. ZERO `Ticker arm FAILED` ESP_LOGE, ZERO new `skipping fast-cadence re-arm` guard fires (the 07:23:36 CDT largest_free=236B fire remains the lone empirical validation). Window producer continuity intact: 218 health samples across 4.4h, cadence transitions all clean at free_heap ~41.6K. Soak now 30.3h continuous — well past the 24h target.

## Recently completed (log-audit-20260518-001, 2026-05-19)

- **codebase-audit-20260517-005** (P3) — extracted the in-callback adaptive-cadence block of `homekit_health_log` (slow/fast cadence decision, 5-min fast-hold, cadence log line, spinlock-guarded Ticker re-arm) into a new `static void homekit_health_update_adaptive_cadence(uint32_t freeHeapNow)` helper, paired-named with `homekit_health_arm_fast_mode_if_low`. Pure mechanical extraction — `freeHeapNow` is the only parameter crossing the boundary; all other state is file-scope. `homekit_health_log` dropped from ~324 to 257 raw lines. The earlier "wait for the deferred-arm pattern" coupling is moot — codebase-audit-20260517-002 shipped a spinlock, not a deferred flag. Build-verified clean; code-review APPROVED, zero behavior change confirmed.
- **codebase-audit-20260517-003** (P3) — `heap_caps_get_largest_free_block` was called every 1Hz in the `service_timer_loop` hot path for an observability-only value (`maxBlock`) discarded 99.99% of the time. Now computed only when `freeHeap < HOMEKIT_HEALTH_HEAP_WATERMARK`, else `0` — behavior-identical because `homekit_health_arm_fast_mode_if_low` early-returns before reading `maxBlock` on the healthy path. `HOMEKIT_HEALTH_HEAP_WATERMARK` constexpr moved to `homekit.h` so `ratgdo.cpp` shares the constant (no magic-number dup). Drops the hot path from 2 heap-mutex reads/sec to 1. Build-verified clean; code-review APPROVED.
- **codebase-audit-20260517-002** (P3) — homekitHealthTicker cross-task re-arm race. The Arduino `Ticker` wrapper's `_timer` pointer write/free is not atomic, so concurrent `detach()`+`attach_ms()` from loopTask (`homekit_health_arm_fast_mode_if_low`) and the esp_timer task (in-callback adaptive block) could leak an `esp_timer` handle (~100 B) and double-register the callback. Fixed by guarding both detach+attach pairs with a `portMUX` spinlock (`healthTickerMux`), ESP32-only. The queue's preferred deferred-flag pattern was evaluated and rejected by the planner: deferring the loopTask arm to the next esp_timer callback would delay fast-mode entry up to 180s and defeat the heap-watermark trigger's immediate-arm purpose. Boot-time arm left unguarded (single-threaded). Build-verified clean; code-review APPROVED.
- **log-audit-20260518-003** (P2) — **closed: observability-only, no fix.** 30s-cadence 15-25KB heap dips every 6-10 min during iOS idle. The item's acceptance was "classify the allocator, resolves to no action if same allocator as -001/-002." Allocator classification is infeasible for the same reason -002 closed (heap-trace not linkable under `framework = arduino`; failed-alloc callback disqualified by the .86 storm). The dips never crash and recover in a single sample — same self-recovering heap-churn family as -001/-002, just shallower. Folded into log-audit-20260517-001. No firmware change.
- **log-audit-20260518-002** (P1) — **closed: observability-only, no fix.** Sub-1Hz heap transient (`min_free_heap_ever=528 B` on .85) — allocator identification proved infeasible. Both candidate instrumentation mechanisms are dead ends: (a) `heap_caps_register_failed_alloc_callback` is disqualified by log-audit-20260518-004 (the .86 callback caused a 25-fire reboot storm, reverted in .87); (b) heap-trace standalone is not linkable — the precompiled arduino-framework libs ship `CONFIG_HEAP_TRACING_OFF=1`, no `heap_trace.h` header, no `heap_trace_*` symbols; enabling it would require migrating the build from `framework = arduino` to `framework = espidf` (a build-system overhaul, out of scope for a diagnostic item). Per the item's own acceptance, confirmed the existing recovery path is sufficient: 10+ sub-1KB transients across .84/.85 with **zero OOM panics** — the 528B intrinsic watermark proves a dip the device survived. The allocator-identification objective is folded into log-audit-20260517-001 (parent investigation, .86 HAP preflight gate is the shipped read-side mitigation). No firmware change.
- **log-audit-20260518-001** (P0) — TASK_WDT reboot diagnostic capture. Investigation found `CONFIG_ESP_TASK_WDT_PANIC=1` is already enabled via the precompiled arduino-framework libs (`sdkconfig.defaults` is inert under `framework = arduino`), so the existing `panic_handler` + coredump path already captures a TWDT backtrace — the 2026-05-17 event's `/crashlog` was empty only because the following OTA wiped RTC_NOINIT. Added an `esp_task_wdt_isr_user_handler` weak-symbol override (`src/log.cpp`, ESP32-only) that runs in the TWDT ISR before the panic and `esp_rom_printf`s the task running on each core — naming the CPU0 hog, the one signal the coredump alone doesn't give (only IDLE0 is TWDT-subscribed). Plus a 2-line `sdkconfig.defaults` comment marking the inert TWDT keys (they had already caused one misdiagnosis). Build-verified clean; weak-symbol override linked with no `multiple definition` error. Acceptance is latent — verified on the next non-OTA-clobbered reset-reason-6 event.

## Recently completed (.85 cleanup, 2026-05-17, PR #135)

- **log-audit-20260517-002** (P2) — tickDrift -150000ms artifact after cadence transitions. Drift formula now reads currently-armed interval via `__atomic_load_n(&currentHealthIntervalMs, __ATOMIC_RELAXED)`; both re-arm sites zero `lastTickMs` so first post-transition sample reports `tickDrift=0`. Steady-state drift now matches pre-.84 baseline (~100ms). No functional impact (watchdog unaffected); telemetry hygiene only. **Acceptance MET 2026-05-18 audit**: 372/424 post-.85 samples = exactly 0ms, all others ±3ms; pre-.85 portion of same audit window had 721x -150000ms confirming the prior regression and the .85 fix exclusively addresses it.
- **codebase-audit-20260517-001** (P3) — `fastModeEntryMs` / `lastTickMs` int64 cross-task non-atomic. Both now `static volatile _millis_t` with `__atomic_store_n(RELEASE)` / `__atomic_load_n(ACQUIRE)` at all sites. Snapshot-once pattern at multi-check reads. Zero-byte change.

## Recently completed (.84 follow-up, 2026-05-17, PR #132)

- **log-audit-20260515-010 visibility gap** — .83's adaptive sampler missed the 3424B sub-180s dip because the trigger condition was only checked at slow-mode (180s) poll boundaries. .84 adds a 1Hz loopTask heap-watermark check via new `homekit_health_arm_fast_mode_if_low()` helper. Arms fast mode immediately on the drop, independent of poll cadence. Strictly additive — existing in-callback adaptive block continues to handle the stay-fast-5min-after-recovery logic. No FC / dedup / cooldown interactions.

## Recently completed (.83 bundle, 2026-05-16, PR #130 / commit 9dd52ea)

- **log-audit-20260515-010** (P1) — heap visibility: new ESP32-only `GET /heap` endpoint + adaptive HomeKit-health sampler cadence (180s → 30s when free heap < 20KB, holds 5min after recovery). 72h soak acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82, .83 NOT yet flashed)**: NEW deepest-dip-ever observed — heap=4000 maxBlock=2164 at 2026-05-15T19:58:26 CDT (uptime 167403s, ~46.5h). 4000B is 396B above the 4096B heap-floor short-circuit (yesterday's record was 4396, only 300B above). maxBlock=2164 means 54% of remaining heap fragmented into sub-2KB chunks — worst fragmentation snapshot ever recorded. Recovered to 42204 by next 180s sample (so dip lasted <3min). Single user activity in window: HK door open/close ~6 minutes prior (18:21:33 / 18:31:05 CDT) — heap had recovered to 42180 baseline by 19:55:26 then crashed 24KB at 19:58:26 with iOS quiet (last_hap_read_ago=899s, hintLevel=1). RSSI was -43dBm (worst observation in window, baseline -38dBm) suggesting lwIP rx-path or WiFi-stack consumption under marginal signal. SECOND DIP same window: heap=19320 maxBlock=21492 at 23:13:26 CDT, also 3-5min after door operations (Closing 23:03:06, Closing 23:07:50). Deploy of .83 with adaptive 30s cadence and `/heap` endpoint is the right next step — would have captured ~6 fast-samples bracketing the 4000-dip recovery curve. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed earlier today)**: NEW DEEPEST-DIP-EVER on .83 — heap=3424 maxBlock=1908 at 2026-05-16T11:16:18 CDT (uptime 12423s, ~3.4h post-OTA-boot). 3424B is 672B below prior 4000B record, only 672B above the 4096B heap-floor short-circuit. maxBlock=1908 = 56% sub-2KB fragmentation (worst ever, beats yesterday's 54%). last_hap_read_ago=33s (iOS active), RSSI=-37dBm baseline-normal — NOT correlated with marginal-signal hypothesis. No user activity within 30min prior. Recovered to 22064 (next 180s sample) then 39972 (sample after). **CRITICAL .83 FEATURE GAP CONFIRMED**: adaptive sampler 180s→30s acceleration line "HomeKit health sampler cadence: 180000ms -> 30000ms" was NEVER emitted for this dip — only the reverse 30000ms -> 180000ms line at 14:45:19 CDT (after the heap had been steady-state >40KB for 3.5h) appeared, meaning the gate WAS armed during the 11:16 dip but the dip occupied <180s and the next sample already showed 22064 (above the 20KB threshold), so cadence-acceleration ran for ≤1 cycle and the 180→30 announce never logged (or logged but the dip resolved before any 30s sample captured it). Net result: .83 adaptive sampler DOES NOT catch sub-3min transients — the very class of event we deployed it for. Need either (a) much higher poll rate (5-10s) when free heap < 30KB instead of < 20KB, or (b) interrupt-driven heap-low-watermark notification via FreeRTOS `heap_caps_register_failed_alloc_callback` style hook, or (c) sample-on-allocation-failure rather than time-poll. /heap endpoint not exercised in this window (would need on-demand fetch during a future dip — automate via the Pi).
- **log-audit-20260515-011** (P2) — SEC1 retry log enrichment: both first-of-burst and `[+N suppressed]` lines now include `[cmd=0xNN retries=N bus_silent=Nms cts=N rx_pending=N]` for root-cause attribution. 48h structured-log acceptance pending in .83. **RECURRENCE 2026-05-16 audit (still on .82)**: 2 more SEC1 TX retry events at 2026-05-15T23:02:52 and 23:07:37 CDT, both 1.0-1.5s before successful Open→Closing transitions (cmd transition timing identical to prior pattern). Pre-.83 log line still missing diagnostic suffix — confirms enrichment is needed; not a new pattern, just continued observation. Door closed cleanly each time (12047/12049ms). Cumulative SEC1-failure-then-success count now ~10 across .80/.81/.82 with zero functional impact. **RECURRENCE 2026-05-16 18:23 CDT audit (.83 flashed)**: 1 more SEC1 TX retry event at 2026-05-16T19:02:18 CDT (uptime 40386s, ~11.2h post-boot), enriched suffix `[cmd=0x39 retries=11 bus_silent=234ms cts=0 rx_pending=0]` — identical pattern to the 07:52:51 and 08:00:11 enriched events from earlier today's pre-audit window. Fired 1.5s before clean Open→Closing transition (12171ms close, no door reverse, no functional impact). Cumulative .83 enriched-SEC1 events now 3, all `cmd=0x39` (door state query) or `cmd=0x38`, all `retries=11 bus_silent~234ms cts=0 rx_pending=0` — pattern stable: GDO bus is silent for ~234ms during state-change moments, retries exhausted but actual command lands. Acceptance trending toward MET (need ~48h more enriched data).
- **log-audit-20260515-009** (P1) — HK Close-burst dedup + force-close reentry cooldown. New `hk_target_is_redundant()` helper at HK callback layer + `forceCloseClearedAtMs` stamp set in `clear_force_close_state`, gate in `door_command_force_close` (3s window). Strictly additive to existing FC safety gates (.80 release-callback fix, 2-attempt mechanic, CURR_CLOSED/CLOSING/0xFF entry gate, TAS). Code-review verified all 10 FC safety properties PASS.
- **log-audit-20260514-008** (P1) — HK Open-burst dedup: same `hk_target_is_redundant()` helper handles Open path. Mid-cycle reversals preserved.
- **tickDriftMs cast fix** (code-review out-of-scope finding from PR #130 review) — `src/homekit.cpp:688` time math fixed (was producing garbage int32-overflow values at ~25-day uptime).
---

## Upstream filing — DO NOT FILE

The fork's bug fixes (R1-R4 in `audit-notes/UPSTREAM_CHERRY_PICK_PLAN.md`) are already shipped in the fork. Their upstream-applicability is tracked in audit-notes/ for awareness only — **fork work proceeds regardless of upstream interest**.

**Hard rule:** No upstream PRs, no upstream issue filings, no cherry-picks to upstream. Issue #170 was filed historically; do not let it set precedent. Future agents reading this queue: if you find yourself drafting a `gh pr create --repo ratgdo/...`, STOP — that's not how this fork operates.
